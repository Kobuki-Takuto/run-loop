"""RunLoop の本体パッケージ。

周回ランニングコースの生成・選択・表示用データの組み立てを担う。
このパッケージは Streamlit に依存しない（画面は ``ui`` パッケージの責務）。
モジュールの構成と責務は docs/design.md 1.1 / 1.2 を参照。
"""

import time
from typing import Final

# このパッケージが **import された時刻**（プロセスの寿命ごとに1回だけ決まる）。
# `ui/app.py` が「ソースのほうが新しい ＝ 古いプロセスに繋がっている」ことを
# 検出するのに使う（2026-08-07、T18a）。
#
# Streamlit は再実行のたびにメインスクリプトだけを読み直し、import 済みの
# モジュールは `sys.modules` に残す。そのため**画面だけが新しく中身が古い**
# 状態が起こり、実在する関数が「無い」というエラーになる。実機確認で2回続けて
# これに当たったので、手順（再起動を忘れない）ではなく検出できる形にした。
LOADED_AT: Final[float] = time.time()
