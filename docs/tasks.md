# 実装タスク一覧（フェーズ3 の成果物）

- 版: 1.0
- 作成日: 2026-08-05
- 根拠: [requirements.md](requirements.md)（版1.2・確定）と [design.md](design.md)（版1.1）
- 進め方: [WORKFLOW.md](WORKFLOW.md) フェーズ4（1タスク = 1ブランチ、半日以内）

**状態はこのファイルだけで管理する。** 各タスクの先頭の `状態:` を書き換えて進める。
一覧表に状態を持たせないのは、2か所に書くと必ず食い違うため。

| 状態 | 意味 |
|---|---|
| 未着手 | まだ手を付けていない |
| 進行中 | ブランチを切って作業中 |
| 完了 | 変更が main に入り、完了条件をすべて満たした（PR 経由でも main への直接マージでもよい） |

## 着手前に決めたこと（2026-08-05 承認）

1. `streamlit-js-eval` を追加してよい（T15 の直前で追加する）
2. T05（実データ取得スパイク）を実施する。API 約3回の消費を払う
3. CI（GitHub Actions）を T01 で入れる

## 8月12日ごろに確認すること

要検証 #3 のうち「iOS が未使用のサイトデータを消すか」は未実施（スパイク5から数時間しか
経っていないため）。**永続化の実装（T14 / T15）はこの結果に依存しない形に割ってある。**
方式が変わった場合の差し替え範囲は T15 だけで、T14 のポートと検証ロジックは変わらない。

---

## 一覧（依存順）

| # | タスク | ブランチ | 依存 |
|---|---|---|---|
| T01 | パッケージ骨格と CI | `chore/skeleton-ci` | — |
| T02 | models.py と geo.py | `feat/models-geo` | T01 |
| T03 | ports.py | `feat/ports` | T02 |
| T04 | config.py | `feat/config` | T03 |
| T05 | 実データ取得と fixture 化（**API 約3回**） | `spike/fixture-points3` | T01 |
| T06 | ors/mapper.py | `feat/ors-mapper` | T05, T03 |
| T07 | ors/client.py | `feat/ors-client` | T06, T04 |
| T08 | generation.py 二段投入（経路B） | `feat/generation-two-stage` | T07 |
| T09 | generation.py 一括並列（経路A）と乖離検出 | `feat/generation-cached-path` | T08 |
| T10 | selection.py | `feat/selection` | T02 |
| T11 | checkpoints.py | `feat/checkpoints` | T06, T02 |
| T12 | messages.py | `feat/messages` | T02 |
| T13 | session.py | `feat/session` | T10 |
| T14 | persistence.py ポートと純ロジック | `feat/persistence-core` | T02 |
| T15 | localStorage 実装（`streamlit-js-eval`） | `feat/persistence-localstorage` | T14 |
| T16 | ui/map_view.py | `feat/map-view` | T02 |
| T17 | ui/app.py 起点の確定フロー | `feat/app-origin` | T15, T16, T12, T07 |
| T18a | ui/app.py 実行と結果表示 | `feat/app-run` | T17, T13, T09, T11 |
| T18b | ui/app.py 引き直しと例外処理 | `feat/app-reroll-errors` | T18a |
| T19 | 結合確認 | `test/integration-check` | T18b |

T10〜T14 は互いに独立で、T02 が終われば並行できる（T10 は T08 の `GenerationOutcome` の
型だけ共有する）。**T18a が終わった時点で「コースが出るアプリ」になる。**

## 共通の完了条件（全タスク）

- [ ] `uv run pytest` / `uv run ruff check .` / `uv run mypy .` が通る（design.md 10.5）
- [ ] 差分を自分で読んで理解している（WORKFLOW.md フェーズ4）
- [ ] 1タスク = 1ブランチ、半日以内

---

## T01: パッケージ骨格と CI

状態: 完了

### 目的

`runloop/` と `ui/` の空構造を作り、検証コマンド3点と CI を通す土台を置く。

### 触るファイル

`runloop/__init__.py`, `ui/__init__.py`, `tests/`, `pyproject.toml`（ruff / mypy / pytest 設定）,
`.github/workflows/ci.yml`, `main.py`（雛形を撤去）

### 依存

なし（最初のタスク）

### 完了条件

- [x] `uv run pytest` / `uv run ruff check .` / `uv run mypy .` の3つが通る（design.md 10.5「各タスクの完了条件とする」）
- [x] push で CI（ruff + mypy + pytest）が走り、緑になる（WORKFLOW.md フェーズ6 の雛形）
- [x] 非機能要件「API キーをソースコードおよびリポジトリに含めない」を満たす（`.env` が git 管理外であることを再確認。`git ls-files .env` が空）
- [x] `ui/` 以外に `import streamlit` が無い（design.md 1.2 の規律。lint で守れる形にする）
  - ruff の `flake8-tidy-imports` で `streamlit` / `streamlit_folium` を禁止し、`ui/**` だけ除外
  - `tests/test_layering.py` でも二重に固定（設定を緩めたときに静かに崩れないように）

### 実装メモ

- `main.py`（`uv init` の雛形）を削除した。Streamlit の入口は T17 以降の `ui/app.py`
- `spike/**` は ruff の `ANN` / `E501` / `TID251` を除外した。**`E501` を外したのは、
  design.md 3.3 が spike のコードを行番号つきで引用しており、整形で行がずれると
  引用が合わなくなるため**
- mypy は `strict = true`、`spike/` は対象外（使い捨てで本体から参照しない）
- pytest に `pythonpath = ["."]` を入れた（`runloop` / `ui` をインストールせずに import する）
- テスト関数にも `-> None` を書く（mypy strict と CLAUDE.md「型ヒントを必ず書く」に合わせ、
  ruff 側の例外は置かない）

### 参照

design.md 1.1 / 1.2 / 10.5、WORKFLOW.md フェーズ0・6

### ブランチ

`chore/skeleton-ci`

---

## T02: models.py と geo.py（合計距離を計算プロパティにする）

状態: 完了

### 目的

データ構造と座標計算を置く。合計距離は**計算プロパティ**とし、判定値と表示値が同一であることを
構造で保証する。

### 触るファイル

`runloop/models.py`, `runloop/geo.py`, `tests/test_models.py`, `tests/test_geo.py`

### 依存

T01

### 完了条件

- [x] AC-01-2「表示されたコースの**合計距離**（ループ距離 + 接近距離 × 2）が、目標距離の ±300m 以内である」→ `total_m` と `is_within_tolerance` のテスト
- [x] AC-01-5「合計距離が目標距離の3倍を超える候補は異常値として除外する」→ `is_degenerate` のテスト
- [x] AC-02-2「距離誤差（合計距離 − 目標距離）がメートル単位で、符号付きで表示される」→ `error_m` の符号のテスト
- [x] 境界値4件が design.md 5.1 の表どおり（誤差 300.0 は在庫に入れる／ちょうど3倍は異常値にしない／接近 50.0 は OK／300.0 は WARN）
- [x] `geo.haversine` が既知の2点で期待値を返す（スパイクと同じ球近似）
- [x] 丸めを一切しない（表示の丸めは T12 の責務。design.md 2.2）
- [x] `frozen=True` のデータクラスで、`target_m` を `Candidate` に焼き付けている（design.md 2.2）
- [x] テストを先に書き、失敗を確認してから実装した

### 実装メモ

- **置いた型を絞った。** `LatLon` / `RouteQuery` / `ApproachVerdict` / `Candidate` と
  `geo.haversine` のみ。design.md 2.1 の残りは、対応する AC を持つタスクで足す
  （`Turn` / `Checkpoint` → T11、`ProviderRoute` / `RawStep` → T03、
  `GenerationOutcome` / `SelectionResult` → T08 / T10）。
  **テストの根拠がない型を先に置かない**という判断
- **`Candidate.turns` は T11 で追加する。** design.md 2.1 はフィールドに挙げているが、
  `Turn` 型が T11 の成果物のため今回は持たせていない
- **閾値の定数を `models.py` に置いた**（`TOLERANCE_M` / `DEGENERATE_FACTOR` /
  `APPROACH_OK_M` / `APPROACH_REJECT_M`）。design.md 1.3 が「`models.py` は何にも
  依存しない」と定めているため、`Candidate` のプロパティから `config.py` を
  import できない。**T04 は `config.py` からこれを参照して公開する**
- 接近距離の分類（`classify_approach`）もここに置いた。境界値（50.0 は OK、
  300.0 は WARN）が design.md 5.1 の表に属し、定数と同じ場所にあるほうが揺れない
- `geo.haversine` の期待値は実装から写さず、球面上の子午線・赤道の距離が
  `R × 弧度` になる解析的性質から出した（遠距離1件のみ球面余弦定理で照合）。
  **haversine の式をテストにも書くと、実装のバグが同じ形でテストに入る**
- `pyproject.toml` に `[tool.ruff.lint.isort] known-first-party` を1行追加した
  （`runloop` / `ui` が外部ライブラリと同じブロックに並んでいたため）
- 実装後、故意に壊した5パターン（判定前の丸め・3倍の等号・接近 50.0 の等号・
  緯度経度の取り違え・接近距離を1回しか足さない）でテストが落ちることを確認した

### 参照

design.md 2.1 / 2.2 / 2.3 / 5.1、requirements.md AC-01-2 / AC-01-5 / AC-02-1 / AC-02-2

### ブランチ

`feat/models-geo`

---

## T03: ports.py（RouteProvider Protocol とドメイン例外）

状態: 完了

### 目的

外部依存の境界を1か所に置く。上位層に `requests` と HTTP ステータスを持ち込まない。

### 触るファイル

`runloop/ports.py`, `runloop/models.py`（`ProviderRoute` / `SnapResult` を追加）,
`tests/test_ports.py`

### 依存

T02

### 完了条件

- [x] `RouteProvider` Protocol に `round_trip()` と `snap()` の2メソッドがある（design.md 3.1）
- [x] design.md 3.2 の翻訳表の6例外（`ApiKeyMissing` / `ApiKeyRejected` / `RouteNotFound` / `RateLimited` / `ProviderUnavailable` / `MalformedRoute`）が定義され、基底 `RouteProviderError` を共有している
- [x] フェイクのプロバイダが Protocol に適合し `mypy` が通る
- [x] 異常ルート（合計距離が目標の3倍超）を例外にしていない。AC-01-5「除外した候補は AC-01-4 の妥協パスでも表示しない」は選択側（T10）の責務
- [x] プロバイダが目標距離・±300m・3倍・接近距離の閾値を知らない（design.md 3.1「責務の境界」）
- [x] テストを先に書き、失敗を確認してから実装した

### 実装メモ

- **`ProviderRoute` と `SnapResult` は `models.py` に置いた。** Protocol の戻り値型なので
  T03 で必要になるが、design.md 1.2 は `ports.py` を「Protocol とドメイン例外の定義」と
  定めており、データ構造は `models.py` の担当。`LatLon` / `RouteQuery` / `Candidate` と
  同じ場所に揃える判断（2026-08-06 承認）
- **`ProviderRoute.steps`（`RawStep`）は T06 で追加する。** design.md 2.1 はフィールドに
  挙げているが、maneuver 種別をドメインの型でどう表すかの判断材料が T06 の fixture と
  T11 のホワイトリストにある。**T02 で `Candidate.turns` を T11 に回したのと同じ理由**で、
  テストの根拠がない型を先に置かない
- `ProviderRoute` に `approach_m` を持たせていない。接近距離は起点との差であり
  プロバイダの成果物ではない（design.md 3.1）。テストでフィールドの不在を固定した
- `RouteProviderError` が `ratelimit_remaining` をキーワード専用で受ける
  （メッセージの第2引数と取り違えないため）。既定は `None` で、代わりの数を埋めない
- `RouteProvider` を `runtime_checkable` にした。**メソッドの有無しか見ない**ので
  静的な適合は mypy が担保する。テストでは「`snap()` を欠いた実装が適合しない」ことに使う
- **例外がちょうど6件であることをテストで固定した。** 1件ずつ `issubclass` を見るだけだと、
  あとから「異常ルート用の例外」が増えても気づけない（AC-01-5 の除外は T10 の責務）
- 実装後、故意に壊した7パターン（Protocol から `snap()` を落とす・7件目の例外を足す・
  基底から外す・残数を落とす・ドメイン閾値を import する・`approach_m` を持たせる・
  `frozen` を外す）で、意図したテストが落ちることを確認した

### 参照

design.md 3.1 / 3.2、requirements.md 非機能要件（保守性）

### ブランチ

`feat/ports`

---

## T04: config.py（定数と設定。フォールバック値を置かない）

状態: 完了

### 目的

要件由来の数値を1か所に集め、設定の欠落で**静かに間違えない**ようにする。

### 触るファイル

`runloop/config.py`, `tests/test_config.py`

### 依存

T03

### 完了条件

- [x] 定数（±300m / 15本 / 50m・300m / TTL 30日 / 乖離 10m / タイムアウト 8秒 / 送信上限 18回）が `config.py` にあり、コード中に散っていない
- [x] AC-06-2「API キーが未設定の場合、設定が必要である旨のメッセージが表示される」→ キー欠落時に**1回も送信せず** `ApiKeyMissing`
- [x] `os.getenv(...) or <定数>` および `.get(key, <それらしい既定値>)` の形が `runloop/` に無い（design.md 3.3。`spike/` のフォールバック座標を本体に持ち込まない）
- [x] 起点を環境変数から読まない（起点はブラウザの永続化のみ。design.md 3.3 の表）
- [x] 欠落時のメッセージにキー名と探した場所が入り、値そのものはログにも画面にも出ない
- [x] テストを先に書き、失敗を確認してから実装した

### 実装メモ

- **読み出し元を `load_settings(env)` の引数で受ける形にした**（2026-08-06 承認）。
  design.md 3.3 はキーの出所を「`.env` / `st.secrets`」としているが、`runloop/` は
  Streamlit を import できない（design.md 1.2）ため受け渡し方が未定だった。
  `ui/` が `st.secrets` を渡し、引数を省いたときだけ `.env` と `os.environ` を見る。
  **design.md 3.3 に判断を追記した**（`ui/` が `os.environ` へ書き写す案を採らない理由つき）
- 閾値4件は `models.py` から import し、**構文木で「代入していない」ことを固定した。**
  値の一致だけを見るテストは、同じ数値を書き写しても通ってしまう（T02 の申し送り）
- **「定数が散っていない」の検査を構文木（AST）で行った。** ソースの文字列検索では
  docstring の「15本投げる」のような説明文に反応する（実際 `ports.py` にこの記述がある）。
  `ast.Constant` の数値だけを見れば説明文と区別できる。値だけでなく型も比較している
  （`10` と `10.0` は `==` で等しいため、無関係な整数を乖離の許容値と誤認しないように）
- `.get(key, <既定値>)` を禁止しているので、`config.py` 自身も `in` で有無を見てから
  `[]` で読む。**禁止の対象に自分を含めた**（テストは `runloop/**` 全体を走査する）
- `Settings.api_key` は `repr=False`。データクラスの既定の `repr` は全項目を出すため、
  この型が例外メッセージやログに混ざるとキーが漏れる。型の側で隠す
- 空白だけのキーを欠落として扱う（`.env` に `ORS_API_KEY=` と書いただけの状態）。
  通すと送信して 401 になり、AC-06-2 の「設定が必要」を伝えられない
- 値だけでなく**関係も2件固定した。** `MAX_SEND_ATTEMPTS > CANDIDATE_COUNT`（逆転すると
  1回のリトライで上限を破る）と `SNAP_RADIUS_M > APPROACH_REJECT_M`（半径を 300 に
  合わせると拒否すべき起点が「道路なし」の変種に落ち、AC-01-3 の文言を距離つきで出せない）
- **非機能要件「毎分40回未満」は定数にしていない**（2026-08-06 承認）。使う側
  （T08 以降）がまだ無く、T19 の確認項目のため、使う場所ができた時点で足す
- 実装後、故意に壊した10パターン（閾値の再定義・`.strip()` の除去・メッセージから
  キー名を落とす・`repr=False` の除去・既定キーへのフォールバック・`Settings` に
  起点を足す・`HOME_LAT` を読む・`ports.py` に 350 を書く・送信上限を15に・
  半径を300に）で、意図したテストが落ちることを確認した

### 参照

design.md 3.3、requirements.md AC-06-2、非機能要件（セキュリティ）

### ブランチ

`feat/config`

---

## T05: 実データ1件の取得と fixture 化（API を約3回消費）

状態: 完了

### 目的

points=3・`instructions: true` の実レスポンスと `/v2/snap` の実レスポンスを1件ずつ取得し、
要検証 #4 / #12 / #13 を埋める。

**API を約3回消費する**（directions 1回、`/v2/snap` 1〜2回）。2026-08-05 に承認済み。

### 触るファイル

`spike/`（使い捨てスクリプト）, `tests/fixtures/`（追加）, `docs/design.md` 11節, `spike/FINDINGS.md`

### 依存

T01（T02〜T04 と並行してよい）

### 完了条件

- [x] `tests/fixtures/` に points=3・`instructions: true` の directions レスポンスが1件ある
      → `ors_round_trip_5km_points3.json`（258 座標・71 step・47KB）
- [x] fixture に自宅座標が含まれていないことを確認済み（design.md 10.3 と同じ扱い）
      → 最も近い座標でも自宅から 1.84km。座標の文字列一致もなし
- [x] 要検証 #4（step 数・`name` が `"-"` の割合・maneuver の分布）に実測値が入る
      → 71 step・`"-"` が **100%**・方向転換 59 件・**type 13 が5件**
- [x] 要検証 #12（`/v2/snap` のスナップ先が directions と一致するか）に結論が入る
      → **完全一致**（座標が同一、`snapped_distance` と haversine の差 0.010m）
- [x] 要検証 #13（`/v2/snap` が directions と同じ無料枠を消費するか）に応答ヘッダの残数で結論が入る
      → **消費する（1回あたり1）。** ただし「directions と同じ枠か」は未確定。
      切り分けには directions をあと1回要する。**設計判断には影響しない**（下記）
- [x] 消費した呼び出し回数と残数の推移を FINDINGS に記録した
- [x] スクリプトは `spike/` に置き、本体（`runloop/`）から参照しない
      （依存は spike → runloop の向きのみ）

### 実装メモ

- **送信5回・消費4回だった**（予定は「約3回」）。超えた理由は2つ。
  **(a) snap に `/geojson` を付けて 406 になった**（`code: 8007`。**枠は消費していない**）。
  directions が `/geojson` を取るので同じ形だと思い込んだのが誤り。
  **(b) 残数ヘッダの解釈が1回では確定せず snap を2回投げた**（tasks.md が
  snap を「1〜2回」と見込んでいた範囲内）
- **`/v2/snap` は GeoJSON を返せない。** 素の `/v2/snap/{profile}` に
  `Accept: application/json` で投げ、`locations[0]` を読む（圏外は `null`）。
  T07 の実装に直接効くので design.md 4.6.1 と 11節 #15 に書いた
- **type 13 が5件実測された。** design.md 7.1 が「未観測」としていた種別で、
  **AC-04-4 のホワイトリスト方針が仮定ではなく実測に裏づけられた。**
  ブラックリスト方式なら、この5件が「起点から N km 地点を不明」として案内に出ていた
- **`name` が 71/71 = 100% で `"-"` だった**（スパイク1 の points=5 では 95%）。
  AC-04-4 の「名前は取得できた場合のみ併記」は例外処理ではなく**通常の経路**
- #12 は完全一致だったが、**8.5.1 の乖離検査は外さない。** 1起点・1時刻の観測であり
  「スナップ先は起点だけで決まる」の証明ではない
- **スクリプトは `HOME_LAT` / `HOME_LON` を読まない。** 他の spike 4本は未設定時に
  暫定座標へ落ちる形だが、fixture はコミットされるため「未設定なら暫定座標」では
  自宅座標が混ざる余地が残る。**設定されていても暫定座標を使う**形にした
- `runloop.geo.haversine` を import して距離を測った（#12 は「本体が算出する接近距離と
  snap の値が一致するか」の検証なので、同じ関数で測らないと比較の意味が薄れる）。
  そのため実行は `uv run python -m spike.ors_points3_snap`
- `--from-saved <STAMP>` で保存済みの応答から分析だけやり直せるようにした。
  **無料枠を使う検証では、分析の書き直しで投げ直さないことが節約になる**
- ループ距離は 4479.4m（目標 5000m に対して **-520.6m**）。points=5 の実測は
  +1177.4m で符号が逆。**points が距離のズレの向きに影響する可能性**があり、
  ADR-0003 の補正の前提に関わる。要検証 #1 と合わせて見る材料として記録した

### 参照

design.md 7.3（要検証）/ 10.3 / 11節 #4・#12・#13・#15

### ブランチ

`spike/fixture-points3`

---

## T06: ors/mapper.py（GeoJSON → ProviderRoute）

状態: 完了

### 目的

ORS のレスポンス形式に依存する知識を `ors/` の中だけに閉じる。

### 触るファイル

`runloop/ors/mapper.py`, `tests/test_ors_mapper.py`,
`runloop/models.py`（`Maneuver` / `RawStep` を追加、`ProviderRoute.steps`）,
`docs/design.md` 7.1 / 7.3 / 10.3 / 11節, `spike/FINDINGS.md`（訂正6）

### 依存

T05（fixture）, T03

### 完了条件

- [x] fixture の JSON から `ProviderRoute` が組める（ループ距離・`ascent`／`descent`・`snapped_start`・steps・3次元座標。design.md 10.2）
- [x] AC-04-4「`"-"` は「名前なし」を意味する ORS の値であり、そのまま画面に出さない」→ `None` に正規化する。警告もログも出さない
- [x] maneuver 種別を型に変換している（方向転換かどうかの判定は T11 のホワイトリスト）
- [x] 想定外の JSON で `MalformedRoute` になる（design.md 10.2「壊れた JSON」）
- [x] ORS 固有の知識（キー名・`"-"`・type 番号）が `ors/` の外に漏れていない
- [x] テストを先に書き、失敗を確認してから実装した

### 実装メモ

- **type 13 は「不明」ではなく「右側維持」だった**（2026-08-06 の訂正）。fixture の
  同じ step の `instruction` が `"Keep right"` で、type 12（`"Keep left"`）の対である。
  T05 は `type` と件数だけを数え、隣のキーを読んでいなかった。
  `Maneuver.KEEP_RIGHT` に変換する（`UNKNOWN` に潰さない）。
  **ホワイトリスト（AC-04-4）の結論は変わらない**が、「意味の分からない番号が
  5件来た」という裏づけは取り下げた。根拠は実測ではなく AC-04-4 の要求そのもの。
  design.md 7.1 / 7.3 / 10.3 / 11節 #4 と FINDINGS 訂正6（第3次）に反映した
- **`Maneuver` は値に番号を持たない**（`TURN_LEFT = "turn_left"`）。番号との対応表は
  `ors/mapper.py` だけが持つ。`TURN_LEFT = 0` にすると ORS の番号体系が
  ドメインの型に焼き付き、別サービスを足すときに `models.py` を触ることになる
- **どれが方向転換かは `models.py` でも `ors/` でも決めていない。** ホワイトリストは
  T11 の責務。要件由来の規則を、プロバイダの都合を閉じる層に置かないため
  （`Maneuver.is_turn` を置いていないことをテストで固定した）
- **`seed` と `ratelimit_remaining` は引数で受ける。** どちらも応答の本文にない。
  seed をレスポンスの `metadata` から読むと「応答が要求を反映している」という
  未検証の仮定が増える。残数は HTTP ヘッダにあり本文からは読めない
- **`RawStep` は `position: LatLon` を持つ**（`way_points` の添字ではない）。
  添字の解決を `checkpoints.py` に持ち出すと、ORS 固有の知識が `ors/` の外に出る。
  **負の添字は Python では末尾から数えられ静かに別の地点を指す**ので範囲を検査する
- **`ProviderRoute.steps` の既定は空タプル。** 方向転換0件は異常ではない
  （design.md 7.3）。ただし `steps` の**キー自体が無い**のは `MalformedRoute` にして、
  「案内が無いルート」と「案内を読み落とした変換」を区別する
- **`/v2/snap` の応答の解析は T06 に入れていない。** T06 の完了条件はすべて
  `ProviderRoute` の話で、snap は T07 の完了条件側にある。T02 / T03 と同じく
  テストの根拠がある場所で足す
- `.get(キー, 既定値)` を使わず `in` で有無を見てから `[]` で読む（T04 の禁止の対象に
  `runloop/` 全体が入っている）。種別の変換も `.get(番号, UNKNOWN)` ではなく
  `in` で書いた。設定の取りこぼしを静かに埋める形と見分けがつかなくなるため
- **メッセージの検査を2件足した**（故意破壊で見つけた穴）。キーの欠落を検査せずに
  読んでも、下流の型検査が捕まえるので `MalformedRoute` にはなる。**変わるのは
  メッセージだけ**なので、「欠落」と「型違い」で違うことを言うこと、
  配列の場所に文字列が来たとき「壊れているのは配列そのもの」と言うことを固定した。
  200 が返っているのに読めないとき、手がかりはメッセージしかない
  （応答の本文をログに出すと座標が漏れる。design.md 8.7）
- 実装後、故意に壊した16パターン（緯度経度の取り違え・`"-"` を残す・`"-"` の
  ログ出力・type 13 を `UNKNOWN` に・未知の種別を例外に・添字の範囲検査の除去・
  欠落キーを `None` で埋める・`steps` のキー欠落を0件扱い・距離を累積に・
  `snapped_start` を2点目に・`ValueError` を上げる・メッセージに座標・
  文字列を配列として通す・`seed` の引数を無視・`Maneuver` の値を番号に・
  ORS のキー名を `models.py` に持ち出す）で、意図したテストが落ちることを確認した

### 参照

design.md 1.2 / 7.1 / 10.2、requirements.md AC-04-4

### ブランチ

`feat/ors-mapper`

---

## T07: ors/client.py（1回の呼び出しとステータスの翻訳）

状態: 完了

### 目的

ORS への1回の呼び出しに責務を閉じる。並列化と本数の管理は T08 の仕事。

### 触るファイル

`runloop/ors/client.py`, `tests/test_ors_client.py`,
`runloop/ors/mapper.py`（`to_snap_result` を追加）, `docs/design.md` 4.3

### 依存

T06, T04

### 完了条件

- [x] AC-03-1「生成されるコースに階段が含まれない」→ `avoid_features: [steps]` を必ず送る（`responses` で検証）
- [x] AC-06-1「API 呼び出しが失敗した場合、失敗した旨のメッセージが表示される」の前提として、404→`RouteNotFound`、429→`RateLimited`、401/403→`ApiKeyRejected`、5xx／接続／タイムアウト→`ProviderUnavailable` に翻訳される
- [x] リトライ回数のテスト: 429 は2回送信、404 は1回だけ（`responses` の呼び出し回数で検証）。**404 は無料枠を消費するため投げ直さない**（design.md 4.3 / requirements.md 4.6.1）
- [x] 429 の待機は1秒。回復しなければ欠測として続行する（10秒の性能要件と衝突させない）
- [x] タイムアウトが8秒で、応答待ちで固まらない
- [x] `snap()` を半径 350 で投げ、`null`（半径内に道路なし）を区別して返す（design.md 4.6.1）
- [x] `ratelimit_remaining` を `ProviderRoute` と例外が持ち回る。画面には出さずログに出す
- [x] 例外メッセージにリクエストヘッダ（= API キー）を含めない（非機能要件・セキュリティ）
- [x] 外部 API を実際に叩くテストを書いていない（`responses` でモック）
- [x] テストを先に書き、失敗を確認してから実装した

### 実装メモ

- **`/v2/snap` の応答の解析は `ors/mapper.py` に置いた**（`to_snap_result`。2026-08-06 承認）。
  必要な処理は「`locations[0]` を読む・`null` を区別する・`"-"` を `None` にする・
  読めない形を `MalformedRoute` にする」で、**すべて `mapper.py` の既存部品と同じもの**
  である。`client.py` に書くと `"-"` の知識が `ors/` の中で2か所になる。
  `models.py` の `SnapResult` の docstring も「`"-"` は `ors/mapper.py` が正規化する」と
  定めていた。`_read_name` は `steps[]` と `locations[0]` の両方で使うので
  `where` を引数に足した
- **リトライの判断は観測した事実（429 / 5xx / 接続 / タイムアウト）で行い、例外の型では
  行わない**（2026-08-06 承認、design.md 4.3 に追記）。型で決めると表に無い 400 が
  `ProviderUnavailable` の規則に乗って2回送られ、結果が変わらないまま無料枠を減らす。
  表に無い状態は `ProviderUnavailable` に寄せる（上位は欠測として続行できる。AC-06-4）
- **`avoid_steps=False` のとき `avoid_features` を送らない形にした。** 常に送る実装でも
  AC-03-1 のテストは通ってしまい、「引数が効いている」状態と
  「たまたま既定で満たされている」状態が区別できない
- **半径 350 を client に焼き付けない。** `ports.py` の Protocol が引数で受ける形に
  定めており、値は T17 が `config.SNAP_RADIUS_M` から渡す。`config.py` の
  「定数が散っていない」検査（T04）も `runloop/**` の `350` を禁じている
- **キー欠落（AC-06-2）の再確認を client に入れていない。** `load_settings` が送信前に
  `ApiKeyMissing` を上げ、テストも T04 側にある。テストの根拠がない防御を足さない
  （T02 / T03 / T06 と同じ判断）
- 残数は `int()` で読み、読めなければ `None`。**0 を埋めない**（「枠を使い切った」と
  読めてしまい、観測できていないことと区別がつかない）
- snap の残数は `SnapResult` に載せられない（型が持たない）ので、**ログだけが
  消費の観測手段**になる。snap も枠を消費する（T05 の実測）ため両方でログに出す
- 待機は `time.sleep()` を呼ぶ。テストは `time` モジュールの属性を差し替えて
  待った秒数を検査する。`from time import sleep` で束縛すると差し替えが効かず
  検査が空リストで落ちるので、**呼び方はテスト側から強制される**
- 実装後、故意に壊した29パターン（緯度経度の取り違え・階段回避を送らない・
  `avoid_steps` を無視・snap に `/geojson`・snap を geo+json で受ける・404 を投げ直す・
  429 を投げ直さない・400 も投げ直す・429 で待たない・5xx でも待つ・投げ直しを2回・
  タイムアウトを渡さない・403 を `RouteNotFound` に・残数を 0 で埋める・残数をログに
  出さない・メッセージにヘッダ・ログに本文とヘッダ・`ValueError` を素通し・接続エラーを
  素通し・`instructions` を要求しない・seed／length／points／半径を焼き付ける・
  ベース URL を焼き付ける・snap の `null` を 0m に・0m を圏外に・`"-"` をそのまま・
  欠けたキーを既定値で）で、意図したテストが落ちることを確認した
- **1件だけ穴が見つかり、テストを足した。** `seed` を `1` に焼き付ける破壊が
  最初は通った（テストが `SEED = 1` を使っていたため、引数が効いているのか
  定数なのか区別できなかった）。**2回投げて本文が違うこと**を見るテストを追加した。
  シードが効かないと15本が同じ経路になり、在庫が1本に潰れるが、画面上は
  「コースが1本出る」ので正常に見える（design.md 4.2）

### 参照

design.md 3.2 / 4.3 / 4.5 / 4.6.1 / 10.2、requirements.md AC-03-1 / AC-06-1 / 4.6.1

### ブランチ

`feat/ors-client`

---

## T08: generation.py — 二段投入（経路B）と接近ゲート

状態: 完了

### 目的

プローブ1本 → 接近ゲート → 残り14本を並列。道路から離れた起点で15回消費しないための
打ち切りを作る。

### 触るファイル

`runloop/generation.py`, `tests/test_generation.py`

### 依存

T07

### 完了条件

- [x] AC-01-3「300m 超 → 結果を表示せず拒否する」の前段として、**プローブの応答が 300m 超のとき2本目以降が1回も送信されない**（design.md 10.2 が「テストの中心」と位置づけた項目）
- [x] AC-06-4「いずれの異常時も、アプリが停止せず再実行できる状態を保つ」→ 15本中2本が 404 でも 13 本で選択が続く
- [x] AC-06-3「候補が1件も取得できなかった場合、その旨のメッセージが表示される」→ 全滅（成功0本）で `NO_CANDIDATE` に落ちる
- [x] 非機能要件「1回の実行における外部 API 呼び出しを **15 回**以内に抑える」→ 消費が15回を超えないテスト。**プローブの応答も候補として在庫に入れる**（別枠にすると16回になる）
- [x] 接近距離は `geo.haversine(起点, snapped_start)` で generation が算出し、候補ごとに持たせる（design.md 2.2）
- [x] 並列は `ThreadPoolExecutor`（実測済みの構成。`asyncio` に替えない）。フェイクのプロバイダで同時に投げていることを検証する（design.md 10.4）
- [x] 失敗は種類別に数えて（`failures`）ログに出し、画面には出さない（design.md 4.4）
- [x] シードは実行ごとに乱数で15個（重複なし）。固定しない（design.md 4.2）
- [x] テストを先に書き、失敗を確認してから実装した

### 実装メモ

- **T07 の申し送り「18回の上限は generation.py 側で数える」は実装できなかった**
  （2026-08-06 承認、経路A）。`RouteProvider.round_trip()` が返すのは
  `ProviderRoute` だけで、client の内側で1回送ったか2回送ったかは見えない。
  15本すべてが 429 を引けば送信は30回になりうる。強制するにはポートと6例外に
  送信回数を持たせることになり、HTTP 側の都合がプロバイダの境界を越える。
  **数えるのは消費（`calls_consumed`）だけ**とし、design.md 4.3 を
  「18回を見込む（上限として強制はしない）」に直して理由を残した
- **プローブが失敗したら次のシードで測り直す**（2026-08-06 承認）。design.md に
  記述がなかった点。404 は 1/103〜2/23 の頻度で実測されるので、1本目に当たった
  だけで実行全体が死ぬ経路を残さない。測り直しも15回の枠の内側で数える。
  「測り直したプローブでもゲートが効く」ことを別のテストで固定した——
  失敗が1件なら本数は 14 本のままなので、**ゲートを飛ばす実装でも
  本数のテストだけは通ってしまう**
- **`GenerationOutcome` は `models.py` に置いた**（T03 の `ProviderRoute` /
  `SnapResult` と同じ判断）。design.md 1.2 が `generation.py` を「候補生成の
  手続き」と定めており、データ構造は `models.py` の担当
- **拒否のときもプローブの候補を返す。** 生成側で0本にすると「起点が悪い」と
  「ルートが見つからない」（AC-06-3）が画面上で区別できない（design.md 5.2）。
  表示するかどうかは `selection.py`（T10）が `ORIGIN_REJECTED` で決める
- **`approach_m` / `verdict` は1本も測れなかったとき `None`。** 0m や `REJECT` で
  埋めない。0m は「起点が道路の上」、`REJECT` は「起点が遠い」という別の事実で、
  観測できていないことと区別する（T07 の残数の扱いと同じ判断）
- **失敗はワーカーの中で数えず、例外を持ち帰ってから数える。** `Counter` への
  加算を14スレッドから同時に行うと競合する。`_attempt` は
  `Candidate | RouteProviderError` を返し、集計は呼び出し側の1か所で行う
- **`max_workers` を投げる本数（14）と同じにした。** 少なくすると波に分かれ、
  429 の待機（client の内側の `time.sleep`）が重ならずに積み上がる。
  T07 の申し送り3件目への答えで、**14本が同時に飛んでいること**を
  14人ぶりのバリアで固定した（そろわなければ時間切れで落ちる）。
  壁時計は測っていない（design.md 10.4）
- 残る 10秒の懸念は**二段が直列であること**由来（両段で 429 なら約 12.6 秒）。
  design.md 11節 #16 と T19 の完了条件に記録した
- **キー欠落（AC-06-2）の再確認を入れていない。** `load_settings` が送信前に
  `ApiKeyMissing` を上げ、テストも T04 側にある（T07 と同じ判断）
- 接近ゲートの境界値（50.0 / 300.0）を**このテストでは使っていない。** 接近距離は
  座標から算出されるので、度への変換と `haversine` の往復で
  50.00000000043126 になり厳密な等号を作れない。境界の等号は `models.py` が
  定義元で、T02 のテストが**生の float** で固定している
- **空のスタブを一時的に置いて赤を確認した。** 収集時の `ModuleNotFoundError`
  だけでは「各テストが自分の主張で落ちるか」が分からない。この確認で
  **スタブでも通ってしまうテストが5件見つかり**（1本も投げなければバリアは
  壊れない、消費の検査が `<= 15` だけ、など）、本数の等号を足して締めた
- 実装後、故意に壊した20パターン（打ち切らない・`aborted_early` を False に・
  プローブを候補に入れない・ゲートの境界をずらす・プローブ失敗で実行ごと諦める・
  ワーカー数を減らす・接近距離を末尾の座標から測る・length を補正する・
  シードを焼き付ける・シードの重複を許す・失敗を1種類にまとめる・失敗を例外で
  抜ける・snap も投げる・消費回数を15で埋める・生成側で候補を絞る・ログに座標・
  頂点数を焼き付ける・階段回避を焼き付ける・目標距離を焼き付けない・
  測れなかった接近距離を0mで埋める）で、意図したテストが落ちることを確認した

### 参照

design.md 4.1 / 4.2 / 4.4 / 10.2 / 10.4、requirements.md AC-01-3 / AC-06-3 / AC-06-4 / 7節

### ブランチ

`feat/generation-two-stage`

---

## T09: generation.py — 一括並列（経路A）と乖離検出

状態: 完了

### 目的

スナップ距離のキャッシュが有効なとき、接近ゲートを判定済みとして15本を一括で投げる
（節約するのは呼び出し数ではなく**時間**）。同時に、キャッシュが誤っていたことに
気づく手段を持つ。

### 触るファイル

`runloop/generation.py`, `tests/test_generation.py`

### 依存

T08

### 完了条件

- [x] キャッシュが有効なとき、プローブを挟まず15本が同時に登録される（design.md 10.2「一括並列」／4.6.2 経路A）
- [x] キャッシュがない／失効しているときは二段投入（経路B）に落ちる（同「経路の切り替え」）
- [x] どちらの経路でも directions は15回（非機能要件「15 回以内」）
- [x] 各候補の `approach_m` がキャッシュ値と **10m を超えて**離れていたら、キャッシュ破棄を呼び出し側に通知する（design.md 8.5.1）
- [x] AC-01-2 の判定と表示には**各応答の `snapped_start` から算出した値**を使い、キャッシュ値を流用しない（design.md 2.2 / 8.5.1）
- [x] 乖離を検出したらログに出す（座標は出さない）
- [x] テストを先に書き、失敗を確認してから実装した

### 実装メモ

- **キャッシュは `cached_approach_m: float | None` で受ける**（2026-08-06 承認）。
  失効（30日）・不変則違反・壊れた値の判定は T14 の `persistence.py` の責務なので、
  **`generation.py` は現在時刻を知らない。** `OriginRecord` ごと渡す案を採ると
  同じ失効規則が2か所に分かれ、片方だけ直したときに静かに食い違う。
  `None` は「キャッシュなし・失効・破棄後」をまとめて表し、経路Bに落ちる
- **`GenerationOutcome.cache_diverged` は通知であって破棄ではない。** 破棄するのは
  呼び出し側で、生成側は保存先を知らない（design.md 8.3 のポート）。
  キャッシュを使わなかった実行と、1本も測れなかった実行では `False`——
  比べる相手が無いことと「ずれていた」を区別する（T08 の `approach_m=None` と同じ判断）
- **経路A でも `verdict` と `approach_m` は各応答の実測から出す**（2026-08-06 承認、
  design.md 4.6.3 に追記）。design.md は「キャッシュが誤っていれば15回使う」ことを
  受け入れると書いていたが、**その実行の結果を表示するかを決めていなかった。**
  キャッシュ値（20m = OK）を verdict に流すと、実測 420m の起点のコースが画面に出て
  AC-01-3 を破る。キャッシュは**ゲートを飛ばす判断**にだけ使い、**結論**は実測から出す
- **キャッシュ値が 300m 超だった場合の防御を足していない**（2026-08-06 承認）。
  design.md 8.2 の不変則（ゲートを通った起点だけ保存する）と T14 の完了条件
  （`snapped_distance_m > 300` は破棄）で二重に防がれている。テストの根拠がない
  防御を足さない方針（T02 / T03 / T06 / T07 / T08 と同じ）
- **「プローブを挟まない」は本数では主張できない**（どちらの経路でも15本）。
  **15人ぶりのバリア**で待ち合わせ、1本目を待つ実装なら全員がそろわず時間切れに
  なる形で固定した（T08 の14人ぶりのバリアと同じ手口。壁時計は測らない）
- **乖離の境界（ちょうど 10.0m）は実測値から差を引いて作った。** 座標から算出した
  接近距離では厳密な等号を作れない（T08 が 50.0 / 300.0 で断念した点）が、
  **キャッシュ値の側を `実測 - 10.0` で作れば**差はちょうど 10.0 になる。
  引き算が誤差なく 10.0 を返すことはテスト自身が先に検査する（前提が崩れたら
  「境界を検査したつもり」で通り続けるより落ちるほうがよい）
- **穴が1件見つかり、テストを足した。** 経路Aの `calls_consumed` を
  `len(candidates)` で数える破壊が最初は通った（部分的失敗のテストが消費回数を
  見ていなかった）。失敗した2本も枠を消費しているので、**失敗のある実行ほど
  消費を少なく見積もる**バグになる。等号の検査を足した
- 実装後、故意に壊した21パターン（プローブを挟む・キャッシュ無しでも一括・並列度を
  1に・プローブを別枠で16回・消費を候補数で数える・verdict をキャッシュから・
  approach_m をキャッシュから・候補の接近距離をキャッシュで埋める・乖離を検査
  しない・常に乖離・先頭だけ比べる・プローブを比較から外す・平均で比べる・
  測れないとき乖離扱い・乖離で候補を捨てる・ログを出さない・ログに座標・許容を
  100m に・境界を「以上」に・snap を投げる・シードを固定）で、**21件すべて**
  意図したテストが落ちることを確認した

### 残った申し送り

- `runloop/generation.py` が 368 行、`tests/test_generation.py` が 1211 行で、
  CLAUDE.md の300行の目安を超えている。テスト側は T13 のあとに
  「フェイクで見る構造 / `responses` で見る送信」で分ける（2026-08-06 合意）。
  本体側は経路A・経路B・共通部品が1本の流れなので、分けるなら T18a の実測後に
  「投げる部品」と「結果を組み立てる部品」で見直す
- 経路A の所要（約2秒）は**推計のまま**。15本同時の壁時計は未実測で、
  design.md 11節 #10 と T19 の確認項目に残っている

### 参照

design.md 4.6.2 / 4.6.3 / 8.5.1 / 10.2

### ブランチ

`feat/generation-cached-path`

---

## T10: selection.py（6節の順序をそのまま実装する）

状態: 完了

### 目的

本アプリの中核価値（距離の正確さ）を担う純関数。**距離を先に、標高を後に。**

### 触るファイル

`runloop/selection.py`, `tests/test_selection.py`,
`runloop/models.py`（`SelectionOutcome` / `SelectionResult` を追加）, `docs/design.md` 5.1

### 依存

T02（`GenerationOutcome` の型のみ T08 と共有）

### 完了条件

- [x] AC-01-3 の拒否: `verdict == REJECT` のとき候補を1本も返さず `ORIGIN_REJECTED`（`NO_CANDIDATE` と区別する。design.md 5.2）
- [x] AC-01-5「除外した候補は AC-01-4 の妥協パス（誤差最小の候補を出す経路）でも表示しない」→ 415km が在庫にも妥協パスにも出ない
- [x] AC-03-2「距離条件（合計距離 ±300m）を満たす候補が2件以上ある場合、そのうち獲得標高が最小の候補が選ばれる」→ **標高が小さいが距離が外れた候補が選ばれない**ことのテスト（順序を逆にしていない）
- [x] AC-01-4「±300m 以内の候補が得られなかった場合、距離誤差が最小の候補を表示し、条件を満たすコースがなかった旨のメッセージを表示する」→ `COMPROMISED`、在庫は空のまま
- [x] AC-06-3「候補が1件も取得できなかった場合、その旨のメッセージが表示される」→ 異常値除外後に0件で `NO_CANDIDATE`
- [x] AC-08-1「選択規準は初回表示と同一であり、ランダムではない」→ 並び順（獲得標高 昇順 → |距離誤差| 昇順 → seed 昇順）が決定的。標高が同値でも順序が揺れない
- [x] 境界値（誤差ちょうど 300.0m は在庫、合計距離ちょうど3倍は異常値でない）がテストで固定されている
- [x] 外部依存ゼロ（`requests` も Streamlit も import しない）
- [x] テストを先に書き、失敗を確認してから実装した

### 実装メモ

- **`select()` は `GenerationOutcome` だけを受け取る**（2026-08-06 承認）。design.md 5.1 は
  入力を「`GenerationOutcome`、`target_m`」としていたが、目標距離は `Candidate` に
  焼き付いている（T02、design.md 2.2）。選択側でも受け取ると、**呼び出し側が候補と
  別の目標距離を渡す余地が戻る**。design.md 5.1 を「`GenerationOutcome` のみ」に直した
- **妥協パスの同値時の順序を決めた**（`|距離誤差|` 昇順 → 獲得標高 昇順 → seed 昇順。
  2026-08-06 承認）。design.md は「誤差最小」までしか決めていなかったが、
  候補は並列に集めるので到着順が実行ごとに変わり、`min()` は先に見た方を返す。
  決めないと同じ起点・同じ目標距離で実行ごとに違うコースが出て AC-08-1 を破る。
  第1キーが在庫（標高が先）と違うのは、妥協パスの基準が距離だから。design.md 5.1 に追記した
- **`SelectionResult` / `SelectionOutcome` は `models.py` に置いた**（T03 の `ProviderRoute` /
  T08 の `GenerationOutcome` と同じ判断。design.md 1.2 がデータ構造を `models.py` の担当と
  定めている）
- **閾値の判定をこのモジュールに書いていない。** ±300m も3倍も定義元は
  `Candidate.is_within_tolerance` / `is_degenerate` で、ここが持つのは**順序**だけ。
  不等号を書き直すと境界の等号が2か所に分かれる（T04 が構文木で「定数が散っていない」ことを
  固定しているのと同じ理由。実際、故意破壊で `>=` に変えると境界のテストが落ちる）
- **`chosen` は在庫の先頭と同一オブジェクトにした**（別に選ばない）。別々に選ぶと、
  引き直し1回目で初回と同じコースが出る経路ができる（AC-08-1）
- **空のスタブを一時的に置いて赤を確認した**（T08 / T09 と同じ手口）。収集時の
  `ImportError` だけでは各テストが自分の主張で落ちるか分からない。この確認で
  **スタブでも通るテストが2件見つかった**——「入力順に依らない」が両方とも空でも
  通っていた（期待する並びを直接書いて締めた）ことと、「測れなかった実行は拒否ではない」が
  常に `NO_CANDIDATE` を返すスタブで通っていたこと（同じ0件でも REJECT なら
  結論が変わる対照を足した）
- 実装後、故意に壊した24パターン（ゲートを外す・ゲートを除外の後ろに置く・WARN も拒否・
  `verdict=None` も拒否・拒否のときに除外件数を数える・異常値を除外しない・異常値を
  在庫の段でだけ外す・除外を「3倍以上」に・除外件数を0で埋める・除外後0件を妥協パスに・
  在庫の並びを標高だけで・第3キーを落とす・標高と距離を入れ替える・降順・±300m で
  絞らない・在庫の判定にループ距離だけ・在庫を1本だけ・chosen を先頭と別に選ぶ・
  妥協パスを標高最小で・妥協パスの同値時の順序を落とす・妥協パスで符号付きの誤差を
  比べる・妥協パスで在庫を埋める・在庫を list で返す・`requests` を import）で、
  **24件すべて**意図したテストが落ちることを確認した

### 参照

design.md 5.1 / 5.2 / 10.1、requirements.md 6節 / AC-01-2〜5 / AC-03-2 / AC-06-3 / AC-08-1

### ブランチ

`feat/selection`

---

## T11: checkpoints.py（方向転換の抽出と5件への間引き）

状態: 完了

### 目的

AC-04 の4基準を1か所に集める。接近距離のオフセットを足す場所を型で固定する。

### 触るファイル

`runloop/checkpoints.py`, `tests/test_checkpoints.py`

### 依存

T06, T02

### 完了条件

- [x] AC-04-1「チェックポイントが最大5件表示される」
- [x] AC-04-2「各チェックポイントに、起点からの距離と方向転換の向きが表示される」→ `distance_from_origin_m = approach_m + cumulative_loop_m`。接近距離が**1回だけ**加算される（`Checkpoint` 生成時の1か所のみ。design.md 7.2）
- [x] AC-04-3「方向転換が6か所以上ある場合、起点からの距離が均等になるよう5件に間引かれる」→ ループ長の6等分への最近傍で、結果が決定的
- [x] AC-04-4「未観測の種別（13、ラウンドアバウト系）が来る前提とし、**知らない種別は方向転換として扱わない**側に倒す」→ ホワイトリスト（type 0〜5 のみ）。type 13 が方向転換にならないテスト
- [x] AC-04-4「地点名は取得できた場合のみ併記する。名前がないことは異常として扱わない」→ `None` が画面文言に現れない
- [x] 方向転換が5件以下ならすべて出す。0件でも例外にせず空を返す
- [x] 間引きを通常の経路として実装している（実測では方向転換が76件あった。design.md 7.3）
- [x] テストを先に書き、失敗を確認してから実装した

### 実装メモ

- **`Turn.direction` を `Maneuver` ではなく専用の `TurnDirection`（6件のみ）にした**
  （design.md 2.1 の型どおり）。`Maneuver` には `STRAIGHT` / `KEEP_LEFT` /
  `KEEP_RIGHT` / `DEPART` / `ARRIVE` / `UNKNOWN` という「Turn には絶対に
  現れてはならない値」が混在しており、型を絞ることでホワイトリスト違反を
  実行時の判定ではなく型で表現不可能にした
- **`select_checkpoints` は `loop_m` を明示的な引数で受け取る。** 間引きの
  目標距離（design.md 7.3「`t_i = approach_m + loop_m × i/6`」）にはループ全長が
  必要だが、`Turn` の列はループ上の一部の点でしかなく全長を復元できない。
  `Candidate` / `ProviderRoute` を持つ呼び出し側から渡してもらう
- **接近距離のオフセットを足す場所を型で1か所に固定した**（design.md 7.2）。
  `extract_turns` は `approach_m` を引数に取らない形にし、`Turn` は生の
  累積距離のまま、`Checkpoint` だけが `distance_from_origin_m` を持つ。
  二重加算・加算漏れのどちらも実測（0.7m）では気づけないため、型で防ぐ
- **`Candidate.turns` を models.py に追加した**（T02 の申し送りどおり）。
  T11 の 触るファイル一覧には無いが、design.md 2.1 の `Candidate` の
  フィールド表と、T02 の実装メモ「`turns` は T11 で追加する」に従った
  （T08 が `GenerationOutcome` を models.py に置いた判断と同じ扱い）。
  既定値を空タプルにし、T02〜T10 が作る `Candidate` を壊さないようにした
- **ホワイトリストは辞書（`Maneuver → TurnDirection`）で表現し、辞書に無い
  種別は `None`（Turn化しない）にした。** ブラックリスト（既知の非転換だけ
  除く）にすると、未観測の種別が来たときに「不明」の案内が必要になる
  （design.md 7.1）
- **間引き（`_thin`）は目標距離の昇順に処理し、選んだ `Turn` を候補から
  取り除く。** 取り除かないと、密集した Turn が複数の目標に選ばれて
  重複し、最終的な件数が5件を割り込む（design.md 7.3 手順3）
- **実データ（T05 の fixture、points=3・71 step）から方向転換 59 件になる
  ことを回帰テストにした**（design.md 7.3、2026-08-06 スパイク6の実測値）。
  type 13（Keep right、5件）が方向転換に数えられないことも、実データで
  間接的に確認している
- **穴が1件見つかり、テストを足した。** 間引きの目標距離テストが
  すべて `approach_m=0.0` で書かれており、目標式からオフセットを
  落とす実装ミス（`_thin` 内で `approach_m` を足し忘れる）があっても
  区別できなかった。`approach_m=500.0` の対照テストを追加して締めた
  （T09 の乖離境界の穴と同種の見落とし）
- 実装後、故意に壊した9パターン（type 13 をホワイトリストに含める・
  非転換 step の距離を積まない・累積を終了点にする・`Checkpoint` で
  オフセットを足さない・`_thin` の目標式からオフセットを落とす・
  間引きで選択済み Turn を除外しない・目標の分割数を5等分にする・
  並び順を降順にする・間引き自体を飛ばして全件返す）で、
  意図したテストが落ちることを確認した

### 参照

design.md 7.1 / 7.2 / 7.3 / 10.1、requirements.md AC-04-1〜4

### ブランチ

`feat/checkpoints`（完了、main へ ff マージ済み）

---

## T12: messages.py（画面文言を純関数で返す）

状態: 完了

### 目的

文言の内容そのものが受け入基準なので、テストできる形（純関数）に集約する。
画面に文字列リテラルを書かない。

### 触るファイル

`runloop/messages.py`, `tests/test_messages.py`

### 依存

T02

### 完了条件

- [x] AC-01-3「300m 超のメッセージは……**「最寄りの道路上をクリックしてください」と次にすべき操作を名指しする**」→ 文言に「最寄りの道路上」が含まれる
- [x] 350m 以内に道路がない（`snap` が `null`）変種の文言に、距離が**含まれない**（design.md 4.6.1 / 10.1）
- [x] AC-01-4 の「条件を満たすコースがなかった旨」の文言がある
- [x] AC-02-1「合計距離がキロメートル単位（小数第2位まで）で表示される」→ 丸めはここだけで行う
- [x] AC-02-2「距離誤差……がメートル単位で、符号付きで表示される」
- [x] AC-02-3「不足分を起点付近で調整する案内が表示される」／AC-02-4「超過分を手前で切り上げる案内が表示される」→ 符号で切り替わる
- [x] AC-02-5「接近距離が 50m を超える場合、その値を画面に表示し、**ずれの向きも併せて伝える**」→ 向きは「実走は表示より長くなる」。50m 以下では出さない
- [x] AC-03-3「選ばれたコースの獲得標高が画面に表示される」→ **ループ区間のみの値である旨の注記**が付く
- [x] AC-05-3 / AC-06-1 / AC-06-2 / AC-06-3 の文言があり、design.md 9.1 の対応表を網羅している
- [x] すべての文言が次の行動を含む（design.md 9.2）
- [x] 文言に座標・HTTP ステータス・API キーを含めない
- [x] テストを先に書き、失敗を確認してから実装した

### 実装メモ

- **`messages.py` は `config.py` を参照する形にした**（2026-08-07 承認）。AC-06-2 の文言は
  「設定が必要」だけでなく**キー名と設定場所**を含むため、`API_KEY_ENV_NAME` を
  `config.py` から import する。キー名を文言側に書き写すと定義元が2か所になり、
  名前を変えたときに画面だけ古い名前を案内する。design.md 1.1 の依存図に線を足した
- **距離誤差ちょうど 0 の境界を設計で埋めた**（2026-08-07 承認）。AC-02-3 / AC-02-4 は
  「短い場合」「長い場合」しか定めておらず、ちょうど 0 はどちらでもない。
  どちらかの分岐に倒すと「0m ぶん足してください」を出すことになるため、
  「目標距離ちょうどです」という第3の文言にした。design.md 9.1 の対応表と 9.2 に追記した
- **`all_messages()` を公開関数の列挙から動的に作った。** テスト側が対応表を
  手で持つと、新しい文言を足したのに横断的な規律（次の行動を含む・生の float を
  出さない）の検査から漏れる。`inspect.getmembers` で公開関数と突き合わせ、
  漏れをテスト自身が検知する形にした
- **閾値・例外の翻訳表を丸ごと import し、値を書き写さない。** `APPROACH_OK_M` /
  `TOLERANCE_M` は `models.py`、キー名は `config.py`、例外は `ports.py` の
  定義元をそのまま参照する。境界の等号（50.0 は OK）を messages 側で書き直すと
  T02 の境界と2か所に分かれる
- 実装後、故意に壊した4パターン（接近距離の境界を `<` に・合計距離をループ距離に・
  型ごとの翻訳表を無視して既定文言に固定・以上3点）で、意図したテストが落ちる
  ことを確認した

### 参照

design.md 9.1 / 9.2 / 10.1、requirements.md AC-01-3 / AC-01-4 / AC-02-1〜5 / AC-03-3 / AC-05-3 / AC-06-1〜3

### ブランチ

`feat/messages`（完了、main へ ff マージ済み）

---

## T13: session.py（在庫と引き直しカーソル）

状態: 完了

### 目的

US-08 の状態遷移を Streamlit なしでテストできる純データにする。

### 触るファイル

`runloop/session.py`, `tests/test_session.py`,
`docs/design.md` 2.1 / 6.1 / 6.2

### 依存

T10

### 完了条件

- [x] AC-08-1「引き直しを操作すると、次に良い候補が表示される」→ カーソルが前進のみで進む（「前の候補に戻る」は実装しない）
- [x] AC-08-2「引き直しでは外部 API を呼び出さない」→ 遷移が純データ操作で、プロバイダに触れない
- [x] AC-08-3「在庫を出し切った場合、それ以上の候補がない旨を表示する」→ 末尾でカーソルを動かさない。**尽きたことを黙って同じコースを再表示しない**
- [x] AC-08-4「在庫が尽きたときに ±300m 未満の候補で埋めることは**しない**」→ 妥協パス（`COMPROMISED`）では在庫が空
- [x] 起点または目標距離が変わると在庫を破棄する（`query` を在庫の鍵に持つ。design.md 6.2）
- [x] `RunSession` が1つのオブジェクトで、状態が複数キーに分かれていない（design.md 6.1）
- [x] テストを先に書き、失敗を確認してから実装した

### 実装メモ

- **`RunSession` を不変にした**（2026-08-06 承認）。design.md 2.1 の表は
  `RunSession` だけを「可変」としていたが、6.1 の本文は「1つの不変オブジェクトに
  まとめて差し替えるだけにする」と書いており食い違っていた。6.1 の理由
  （状態を複数のキーに分けると片方だけ更新される事故が起きる）を採り、
  **2.1 の表を「不変」に直して経緯を残した。** Streamlit は操作ごとに再実行され、
  古いセッションを参照する経路が残るので、書き換えるとそこから見える状態が静かに変わる
- **`stock` / `outcome` / `current` はフィールドではなく読み出しにした**（同承認）。
  `RunSession` が持つのは `SelectionResult` で、design.md 6.1 の表もそう直した。
  **妥協パス（AC-01-4）は在庫を空にしたまま1本を表示する**ため、`stock` と
  表示する1本を別フィールドに置くと同じ事実が2か所に分かれる
- **引き直しは `RerollResult(session, advanced)` を返す**（同承認）。新しい
  セッションだけを返すと、末尾では同じ状態が返り画面上は何も起きなかったように
  見える。AC-08-3 の「尽きたことを黙って同じコースを再表示しない」を、
  呼び出し側が見落とせない形にした。末尾では**同一オブジェクト**を返す
  （`is` で固定。カーソルが動いていないことを型の外から確かめられる）
- **在庫の鍵を `RouteQuery` 全体の一致にした**（2026-08-06 承認）。design.md 6.2 は
  「起点または目標距離が変わった」としていたが、`avoid_steps` を外して集めた在庫は
  AC-03-1（階段を含まない）の前提が違い、距離を満たしていても出してよい在庫ではない。
  **破棄すべき条件を数え上げるより条件そのものの一致で見るほうが、条件が増えたときに
  取りこぼさない。** design.md 6.2 に理由つきで書き直した
- **`RunSession` / `RerollResult` は `session.py` に置いた**（`models.py` ではない）。
  design.md 2.1 の表には載っているが、1.2 が `session.py` を「在庫と引き直しカーソルの
  状態（純データ）」と定めており、本タスクの触るファイルも `session.py` だけである。
  T03 / T08 / T10 でデータ構造を `models.py` に寄せたのとは逆の判断だが、
  **この型を使うのは `session.py` と `ui/` だけ**で、ドメインの語彙ではない
- **`generated_at` は引数で受ける**（`datetime.now()` を呼ばない）。T09 で
  `generation.py` に現在時刻を持たせなかったのと同じ判断で、時刻を読む場所を
  `ui/` の1か所に寄せる
- **接近距離は `GenerationOutcome` から取る**（引数で別に受けない）。候補と無関係な
  値を渡す余地を残さないため（T10 が `target_m` を引数から外したのと同じ理由）
- **空のスタブを一時的に置いて赤を確認した**（T08 〜 T10 と同じ手口）。収集時の
  `ModuleNotFoundError` だけでは各テストが自分の主張で落ちるか分からない。
  **スタブでも通るテストが2件見つかった**——`is_stale` が常に `False` を返す実装で
  「条件が同じなら在庫を保つ」が通っていた（同じテストに対照を足した）ことと、
  在庫を空で返す実装で「在庫が tuple である」が通っていた（中身の一致も見るようにした）
- 実装後、故意に壊した26パターン（末尾でも進める・`advanced` を常に True／False・
  末尾で新しいセッションを作る・`current` を常に `chosen`／先頭固定／空なら `None`・
  添字を1つずらす・カーソルを末尾から始める・在庫を並べ直す・接近距離を 0m で埋める・
  生成時刻を自分で読む・`is_stale` を常に False／True／目標距離だけ／起点と目標距離だけ・
  元のセッションを書き換える・引き直しで接近距離／実行条件を落とす・在庫を作り直す・
  `frozen` を外す2件・在庫を list で返す・「前の候補に戻る」を足す・前進のメソッドを
  生やす・プロバイダを import する）で、意図したテストが落ちることを確認した
- **1件だけ穴が見つかり、テストを足した。** 引き直しで `approach_m` を落とす破壊が
  最初は通った。接近距離は AC-02-5（50m 超なら値と向きを画面に表示）の材料で、
  落ちると**2本目から警告が黙って消える**——起点は同じなのに。
  「引き直しで変わるのはカーソルだけ」を固定した

### 残った申し送り

- `RerollResult.advanced` が `False` のときに AC-08-3 の文言を出す配線は T18b。
  文言そのものは T12（`messages.py`）の責務で、design.md 9.1 の対応表に含まれる

### 参照

design.md 6.1 / 6.2 / 6.3 / 10.1、requirements.md AC-08-1〜4

### ブランチ

`feat/session`

---

## T14: persistence.py — ポートと純ロジック（保存先に依存しない）

状態: 完了

### 目的

`OriginStore` の境界と、「読めた値をどう信じるか」の判定を置く。
**この内容は保存先が変わっても変わらない**（8/12 のスパイク5の結果に依存しない）。

### 触るファイル

`runloop/persistence.py`, `tests/test_persistence.py`,
`docs/design.md` 3.3 / 8.3

### 依存

T02

### 完了条件

- [x] AC-07-4「保存された起点がない初回起動時は、起点の指定を促す状態で表示される」→ 3状態の判定（未読→`PENDING`、キーなし→`EMPTY`、値あり→`LOADED`）。`load()` は `None` を返さない（design.md 8.4）
- [x] AC-07-2「次回アプリを開いたとき、保存された起点が復元され地図上に表示される」→ **座標が妥当でスナップ距離だけ壊れているとき、起点は復元される**（design.md 8.6）
- [x] design.md 8.6 の分類がそれぞれ破棄される（読めない／版が不明／欠けている／型が違う／範囲外／`snapped_distance_m > 300` の不変則違反／`probed_at` が不正または未来）
- [x] `probed_at` が30日超のとき、スナップ距離のみ失効し**起点は残る**（design.md 8.5 条件3）
- [x] 壊れた値は画面にエラーを出さず、ログに壊れ方の分類だけ出す。**ログに座標を出さない**（design.md 8.6 / 8.7）
- [x] `save()` が `OriginRecord` を受け取り、座標とスナップ距離が**同時に**書かれる（別々に保存できる形にしない。design.md 8.3）
- [x] `time.sleep()` で読み出しを待っていない（design.md 8.4）
- [x] 座標を丸めていない（AC-05-1「自宅の玄関を正確に起点にしたい」）
- [x] テストを先に書き、失敗を確認してから実装した

### 実装メモ

- **保存する型と読み出す型を分けた**（`OriginRecord` / `OriginLoad`。2026-08-06 承認）。
  design.md 8.3 は `OriginRecord` しか挙げていなかったが、**制約が同じでない。**
  保存はスナップ距離が必須で（同時に書かれることを型で保証する）、読み出しでは
  距離が欠けうる（8.5 条件3 の失効、8.6 の部分復旧）。1つの型で兼ねて
  `snapped_distance_m: float | None` にすると、`save()` が `OriginRecord` を
  受ける目的——座標だけ新しく距離が古い組み合わせを型で防ぐこと——が消える。
  **design.md 8.3 に表つきで追記した**
- **`PENDING` は `decode` が作れない**（`pending()` / `empty()` を別に公開した）。
  読み出しが完了したかどうかを知っているのは保存先の層だけである
  （localStorage では「コンポーネントの戻り値が `None`」がその事実）。
  判定の材料を持たない場所で状態を作らない。`decode(None)` は「読み出しは
  終わったが値が無い」＝ `EMPTY`。8.4 の封筒（`JSON.stringify({v: ...})` と
  二重パース）と保存キーの名前は T15 の担当で、T14 には入れていない
- **`encode()` は時差なしの `probed_at` を受け付けない**（`ValueError`。2026-08-06 承認）。
  「テストの根拠がない防御を足さない」方針（T02 以降）との境界は
  **「起きたときに気づけるか」**で分ける、という基準をもらった。時差なしを通すと
  JST では9時間ずれた瞬間が保存され、症状は「キャッシュが毎回効かない」だけで
  原因に辿り着く手がかりが画面にもログにも残らない。**この判断基準を
  design.md 3.3 に1行残した**（フォールバック値を置かない規律の隣）
- **不変則（`snapped_distance_m > 300`）の検査に `classify_approach` を使い、
  自分で 300 と比べない。** ちょうど 300.0m は WARN で拒否ではないので
  **保存されうる値**である（design.md 5.1）。ここで「300 以上は違反」と書くと、
  書いた側と読んだ側で境界の解釈が食い違う。故意破壊で `distance > 300.0` に
  変えると T04 の「定数が散っていない」検査（構文木）が落ちることも確認した
- **版のキーが無い場合を `UNKNOWN_VERSION` にした**（`MISSING_KEY` にしない）。
  版が付く前の形式で書かれた値がこの形になるので、伝えたい事実は「形式が
  分からない」である。加えて `MISSING_KEY` は**部分復旧する失敗**（距離や日時の
  欠落）にも使う分類なので、混ぜるとログから「全体を捨てたのか距離だけ捨てたのか」が
  読み取れなくなる。テストを書いた時点の想定と実装が食い違い、テスト側を採った
- **緯度経度の値域（±90 / ±180）は `config.py` に置かない。** ±300m や 50m とは
  種類が違い、**変えることが要件の変更にならない**（T04 の定数は要件由来の数値を
  集める場所である）
- **失効はログに出さない。** 壊れているわけではなく通常の経路である
  （起点だけ復元された状態は例外処理ではない。design.md 4.6.2）。破損の分類に
  混ぜると30日ごとに警告が出て、本物の破損が埋もれる
- **空のスタブを一時的に置いて赤を確認した**（T08 〜 T13 と同じ手口）。
  158 件中 105 件が失敗し、**スタブでも通るテストが5件見つかった**——
  「キーなしは `EMPTY`」「壊れた値は距離を捨てる」は常に `EMPTY` を返す実装で通り、
  「ログに座標を出さない」「正常な値でログを出さない」「失効を破損として
  記録しない」は**何もログを出さない実装で自動的に通っていた。**
  それぞれ同じテストに対照（値があるときは `EMPTY` にならない／壊れた値では
  ログが1件出る）を足して締めた
- 実装後、故意に壊した**45パターン**（未読を `EMPTY` に・`PENDING` に起点を持たせる・
  キーなしを `PENDING` に・座標が読めなくても `LOADED`・距離が壊れたら起点も捨てる・
  測れない距離を 0m で埋める・失効を無視／常に失効／境界を「以上」に・TTL を焼き付ける・
  現在時刻を自分で読む・`sleep` で待つ・座標を丸める・緯度経度の取り違え・
  範囲検査を落とす2件／「以上」に・不変則の検査を落とす／WARN も違反に／300.0 と
  直に比べる・負の距離を通す・未来の日時を通す・時差なしを通す2件・UTC に直さない・
  距離／版を書かない・版の欠落を `MISSING_KEY` に・未知の版を通す・真偽値を数値として
  通す・欠けたキーを `None` で埋める・ログを出さない／分類を出さない／座標を出す／
  日時を出す・失効をログに出す・正常な値でログを出す・`load()` を `None` 可に・
  `save()` を `LatLon` に・`clear()` を落とす・既定値を足す・`frozen` を外す2件・
  分類を1つ増やす）で、意図したテストが落ちることを確認した
- **1件だけ穴が見つかり、テストを足した。** **版の検査より先に座標を読む**破壊が
  最初は通った（分類表の各行を1件ずつ壊すテストでは、2か所が同時に壊れている値を
  作れていなかった）。版が違えば `lat` の意味そのものが保証されないので、
  版2が座標を入れ子で持つ形式に変わった場合、ログは毎回「lat が無い」と
  言い続ける。**直すべき場所を指していない手がかりは、無いより悪い**

### 残った申し送り

- 保存キーの名前（`runloop.origin.v1`）と 8.4 の封筒・二重パースは T15。
  `SCHEMA_VERSION` は T14 に置いた（保存形式の版は保存先に依らない）
- `runloop/persistence.py` が 369 行で CLAUDE.md の300行の目安を超えている。
  T15 で実装クラスを足すとさらに伸びるので、分けるならそのときに
  「純ロジック（`decode` / `encode`）」と「localStorage の実装」で見直す

### 参照

design.md 8.2 / 8.3 / 8.4 / 8.5 / 8.6 / 8.7 / 10.1、requirements.md AC-07-1〜4

### ブランチ

`feat/persistence-core`

---

## T15: localStorage 実装（streamlit-js-eval）

状態: 完了

### 目的

`OriginStore` の localStorage 実装。ブラウザ側の事情をここだけに閉じる。

**このタスクで `streamlit-js-eval` を追加する**（承認済み。`uv add streamlit-js-eval`）。

### 触るファイル

`runloop/local_storage.py`（新設）, `tests/test_local_storage.py`, `pyproject.toml`,
`spike/localstorage/check_store.py`（手動確認用）, `docs/design.md` 1.2 / 8.4

### 依存

T14

### 完了条件

- [x] AC-07-1「起点を指定すると、その座標がブラウザに保存される」（手動確認。2026-08-06）
- [x] AC-07-2「次回アプリを開いたとき、保存された起点が復元され地図上に表示される」（手動確認: タブを閉じて開き直しても `LOADED` のまま。2026-08-06）
- [x] AC-07-3「復元された起点は、地図をクリックすることで上書きできる」（手動確認: 別の座標で上書きできた。2026-08-06。**地図のクリックは T17**）
- [x] `get_local_storage` ヘルパを使わず、汎用 JS 評価で 8.4 の3状態を自前で作る（ヘルパは「未読」と「保存なし」をどちらも `None` で返して区別できない）
- [x] Python 側で二重にパースしている（戻り値は `JSON.stringify` の出力がそのまま入る `str`。design.md 8.4 の表）
- [x] **書き込みの直後に `st.rerun()` を呼ばない**（`setItem` が取り消される報告がある。design.md 8.4）
- [x] 座標を URL・ログ・エラーメッセージに出さない（design.md 8.7）
- [x] localStorage への実際の読み書きは手動確認とし、自動テストを書かない（design.md 10.4）
- [x] テストを先に書き、失敗を確認してから実装した

### 実装メモ

- **実装クラスを `runloop/local_storage.py` に分けた**（2026-08-06 承認）。tasks.md は
  `persistence.py` に足す想定だったが、T14 の申し送り（369 行で300行の目安を超えている）に
  対する判断である。分ける線は「**保存先が変わっても変わらないもの**（ポート・8.6 の
  分類・8.5 の失効）」と「**ブラウザ側の事情**（JS 式・コンポーネントの `key`・封筒）」。
  8月12日のスパイク5で方式が変わったとき、**差し替える範囲がファイル1つに一致する。**
  design.md 1.2 の表に行を足した
- **`streamlit_js_eval` を引数で受ける**（既定は本物）。テストはフェイクを渡して
  **投げた式と `key` を記録**する。localStorage そのものは触らないので design.md 10.4 の
  「実際の読み書きは自動テストしない」に反しない——固定しているのは**投げる手前の
  組み立て**である
- **`key` の動かし方を4件テストで固定した。** ここが Streamlit 固有の落とし穴で、
  **どちらの間違いも画面上は正常に見える。** 毎回変えるとそのたびに新しい
  コンポーネントになって `None`（未読）から始まり、**永久に `PENDING`** のまま
  起点が一度も復元されない。変えないと保存直後の画面が保存前の値を映し続け、
  **上書きが効かないように見える**（AC-07-3）。「読むだけなら据え置き、書いたら進める」
- **書き込みは再実行を起こさない**（本体のクラスで実測。design.md 8.4 に追記）。
  `setItem` は値を返さないのでコンポーネントが Python に値を送らない。
  **T17 は、起点を確定した回の描画に `load()` の結果ではなく確定した値そのものを使う**
  （読み直すと1手ぶん古い起点が出る）
- **封筒が壊れていたら `EMPTY` に落とす**（`PENDING` にしない）。`PENDING` は実行ボタンを
  無効にする状態なので、そこに落とすとアプリが操作を受け付けないまま固まる。
  壊れているときの正しい復帰先は初回起動と同じ画面である（design.md 8.6、AC-07-4）
- **分類（8.6）と失効（8.5）をこちらに書かず `decode` に委ねた。** 同じ規則が2か所に
  分かれると片方だけ直したときに静かに食い違う。テストも「委ねていること」だけを見る
- **コンポーネントの `key` の区切りを下線にした。** ハイフンだと f 文字列の中に `"-"` という
  定数が現れ、**ORS の「名前なし」（`"-"`）が `ors/` の外に漏れていないか**を見る
  既存テスト（T06）が反応する。あちらが f 文字列の中まで見るのは意図どおり
  （`f"{name or '-'}"` の形の漏れを捕まえるため）なので、こちらを変えた
- `pyproject.toml` に mypy の除外を1件足した（`streamlit-js-eval` が `py.typed` を
  同梱していない）。**strict は全体では外していない。** 触るのは1行だけで、
  呼び出し側は自前の `JsEvaluator` Protocol で型を付け直している
- **空のスタブを一時的に置いて赤を確認した**（T08 〜 T14 と同じ手口）。30 件中 14 件しか
  落ちず、**スタブでも通るテストが16件見つかった。** うち6件は穴だったので対照を足した
  （「保存なしは `EMPTY`」は常に `EMPTY` を返す実装で、「ログに座標が出ない」「保存で
  ログを出さない」は1件もログを出さない実装で、「読み書きを混ぜない」は1つも式を
  投げない実装で通っていた）。残る7件は `ast` による禁止検査（`rerun` / `sleep` /
  `now` / ヘルパ）と型の形の検査で、**それぞれ対になる振る舞いのテストが赤側にある**
- 実装後、故意に壊した28パターン（未読を `EMPTY` に・未読の分岐を落とす・生のキーを
  読む・`JSON.stringify` を外す・一重パース・文字列でない `v` を通す・保存キーから版を
  落とす・世代を進めない・読み出しでも進める・書き込みの `key` を固定・読み出しの
  `key` を毎回変える・`json.dumps` を外す2件・`encode` を通さない・`removeItem` を
  使わない・読み書きを1式に混ぜる・現在時刻を自分で読む・組み立て時に1回だけ読む・
  `sleep` で待つ・壊れた封筒を `PENDING` に・破損をログに出さない・分類を出さない・
  保存で式をログに出す・保存なしでもログを出す・`get_local_storage` を使う・
  `st.rerun()` を呼ぶ・既定の評価器を差し替える・`key` をハイフンに戻す）で、
  **28件すべて**意図したテストが落ちることを確認した

### 手動確認（AC-07-1〜3）

画面（`ui/app.py`）は T17 の成果物なので、**本体のクラスをそのまま呼ぶ確認用スクリプト**を
`spike/localstorage/check_store.py` に置いた（2026-08-06 承認）。T17 まで先送りすると、
失敗したときに永続化と配線のどちらが悪いか切り分けが要る。**スパイク5は使い捨てコードでの
実測で、本体のクラスでの実測はまだ無い**ことも理由である。

```
uv run streamlit run spike/localstorage/check_store.py
```

Community Cloud と iPhone での確認は T17 でアプリ本体をデプロイしてから行う（同承認）。

**2026-08-06 に実施し、AC-07-1〜3 が成立した。** タブを閉じて開き直しても `LOADED` の
まま復元され、別の座標で上書きできた。**本体のクラスでの実測はこれが最初**である
（スパイク5は使い捨てコードでの実測だった）。3状態の判別も `PENDING` → `EMPTY` の
切り替わりとして観測できた。

**AC-07-3 の「地図をクリックすることで」の部分は T17。** ここで確かめたのは
「復元された起点を上書きできる」ことで、上書きの操作が地図のクリックであることは
`ui/app.py` の担当である。

### 8月12日のスパイク5との関係

要検証 #3 のうち「iOS が未使用のサイトデータを消すか」は 2026-08-12 頃に確認予定。
**方式が変わった場合の差し替え範囲はこのタスクだけ**で、T14 のポートと検証ロジックは変わらない。
なお URL クエリパラメータへの退避は解決にならない（URL も同様に失われる。design.md 11節 #3）。

### 参照

design.md 8.1 / 8.4 / 8.7 / 10.4 / 11節 #3、ADR-0004、requirements.md AC-07-1〜3

### ブランチ

`feat/persistence-localstorage`

---

## T16: ui/map_view.py（folium での地図描画）

状態: 完了

### 目的

地図の描画を Streamlit 層の中で1ファイルに閉じる。

### 触るファイル

`ui/map_view.py`, `tests/test_map_view.py`

### 依存

T02（T11 の `Checkpoint` を表示に使う）

### 完了条件

- [x] AC-05-2「設定された起点が地図上にマーカーで表示される」
- [x] AC-01-1「コースが1本地図上に表示される」のうち描画部分（ループのポリラインとチェックポイントのマーカー）
- [x] 生成された HTML に想定の座標が含まれることの確認（WORKFLOW.md フェーズ4 のテスト方針）
- [x] **接近区間の線を描かない**（レスポンスに存在しない。design.md 4.5 / 7.2）→ 自動テストと実機の両方で確認
- [x] 地図の見た目そのものは手動確認とする（design.md 10.4）→ 一時プレビュー（`preview_map_view.py`、確認後に削除）で実機確認済み。ループが閉じた図形になっている・起点とループの間に余計な線がない・チェックポイントの丸印が3つ表示される、の3点を確認
- [ ] 非機能要件「スマートフォンの縦画面で、指で操作できる大きさの部品を用いる」（手動確認）→ **T17 に持ち越し**（2026-08-07 承認）。`ui/app.py` ができてから実機のスマートフォンでまとめて操作性を確認する。T16 単体の一時プレビューでは判断材料が薄いため

### 実装メモ

- **`build_map(origin, candidate, checkpoints)` の3引数はすべて省略可能にした。**
  起点確定前（何も無し）・起点確定後（origin のみ）・実行後（全部）の3状態を
  1つの関数で表せる。`ui/app.py`（T17）はこの3状態をそのまま呼び分ければよい
- **接近区間の線を描かない、を型ではなく実装の手続きで守った。** `Candidate`
  に接近区間の座標が存在しない（design.md 3.1）ため、`candidate.geometry`
  だけを `PolyLine` に渡す形にすれば、そもそも描きようがない。念のため
  「起点をループの先頭に足す」という改変を故意に入れて、テストがそれを
  検出できるかを確認した（後述）
- **地図の初期中心を実在の地点に焼き付けなかった。** 起点未指定時の表示は
  日本の大まかな中心（緯度経度2桁）にした。この値がユーザーの実際の
  居住地域を示さないようにする判断（プライバシー、CLAUDE.md の非機能要件）
- **方向転換の日本語表記を辞書で持ち、`TurnDirection` の全件数を別テストで
  固定した。** 訳し忘れがあれば `KeyError` で気づける形にし、
  「6件であること」自体もテストで固定した（T12 の失敗の翻訳表と同じ手口）
- **`folium.PolyLine.__init__` に型注釈が無い**（`Marker` / `CircleMarker` /
  `Map` / `Icon` にはある。folium 自身の型定義の抜け）。呼び出し1行にだけ
  `# type: ignore[no-untyped-call]` を付け、理由をコメントに残した。
  strict を全体では緩めていない
- **穴が2件見つかり、テストを座標配列の比較に直して締めた。**
  1つ目: 「起点とループの始点をつなぐ線を足す」という破壊で、既存の
  `html.count("L.polyline") == 1` が通ってしまった（新しい `PolyLine` を
  足したわけではなく、既存の1本に点を足しただけだったため）。
  2つ目: 起点マーカーの緯度経度を入れ替える破壊で、`"31.6..." in html` /
  `"130.5..." in html` の部分一致テストが通ってしまった（両方の数字が
  どこかに存在すれば、順序が違っても一致してしまう）。
  どちらも `L.polyline(...)` / `L.marker(...)` の座標配列を正規表現で
  取り出して JSON として比較する形に直し、直した後で同じ破壊が
  正しく検出されることを確認した
- 実装後、故意に壊した7パターン（起点とループ始点をつなぐ線を足す・
  起点マーカーの緯度経度を入れ替える・起点未指定でもマーカーを描く・
  方向転換の翻訳表を1種類に潰す・地点名が無くても括弧を付ける・
  チェックポイントの描画を丸ごと飛ばす・候補があってもポリラインを
  描かない）で、意図したテストが落ちることを確認した

### 参照

design.md 4.5 / 7.2 / 10.4 / 12節、requirements.md AC-01-1 / AC-04-1〜2 / AC-04-4 / AC-05-2

### ブランチ

`feat/map-view`（完了、main へ ff マージ済み）

---

## T17: ui/app.py — 起点の確定フローと3状態の描画

状態: 完了

### 目的

起点をクリックして確定し、`/v2/snap` で1回プローブして保存するまで。
ここで距離を入れる前に不適切な起点を弾く。

### 触るファイル

`ui/app.py`（新規）, `pyproject.toml`（`[build-system]` の追加）,
`docs/design.md` 1.3（依存の向きの訂正）

**`runloop/config.py` は触っていない。** プロバイダの組み立てを `ui/app.py` に
置いたため（下記の実装メモ）。

### 依存

T15, T16, T12, T07

### 完了条件

- [x] AC-05-1「地図上をクリックすると、その位置が起点として設定される」→ 実機で確認
- [x] AC-05-3「起点を指定せずに実行しようとした場合、指定を促すメッセージが表示される」→ 実機で確認
- [x] AC-01-3 の確定時判定: スナップ距離が 300m 超、または 350m 以内に道路なし（`null`）のとき**起点として確定しない**。directions を**1回も送信しない**（design.md 4.6.3 / 10.2「確定時の拒否」）→ 実機で距離つきの案内が出て確定されないことを確認
- [x] AC-07-4: `PENDING` の間は起点の案内も復元表示もせず、実行ボタンを無効にする（AC-07-2 と AC-07-4 が画面上で混ざらない。design.md 8.4）→ localStorage を空にしたリロードで案内が出て、マーカーが出ないことを確認
- [x] クリックごとではなく**「この地点を起点にする」の確定操作で1回だけ** snap を投げる（design.md 4.6.1）
- [x] 接近ゲートを通った起点だけが保存される（design.md 8.2 の不変則）
- [x] 画面に文字列リテラルを書かず、文言は `messages.py` から取る（**AC が文面を定める案内・エラーのみ。** ページタイトルやボタンの見出しは対象外）
- [x] 画面は手動確認とする（design.md 10.4）
- [ ] AC-06-2「API キーが未設定の場合、設定が必要である旨のメッセージが表示される」→ **T19 に持ち越し**（2026-08-07 承認）。`.env` を触るリスクを避け、結合確認でまとめて見る。実装は入っている（`_load_settings()` が `ApiKeyMissing` を捕まえて `messages.provider_failure()` を出す。送信前に `load_settings()` が上げるので snap は1回も飛ばない）
- [ ] **T16 からの持ち越し** → **さらに T18a に持ち越し**（2026-08-07 承認）。非機能要件「スマートフォンの縦画面で、指で操作できる大きさの部品を用いる」は、**コースが出る状態**で見たほうが実際の使用感に近いため

### 実機確認で見たこと（2026-08-07）

AC-05-1 / AC-05-3 / AC-01-3 / AC-07-2 / AC-07-3 / AC-07-4 の6件。

**AC-07-3（復元された起点の上書き）は、意図せず先に確認できた。** 手順1で
「起点が指定されていません」が出ず、赤いマーカーが出た——T15 の実機確認で
保存した起点が同じキー（`runloop.origin.v1`）で残っていたためである。
つまり手順2〜3で新しい起点を確定した操作が、そのまま AC-07-3 の確認に
なっていた。AC-07-4 は `localStorage.removeItem("runloop.origin.v1")` の後に
リロードして確認した。

**この取り違えは記録に値する。** 実機確認では「保存が残っている」状態が
既定であり、**初回起動を見るには消す操作が要る。** T19 の結合確認でも
AC-07-4 を見るときは同じ手順を踏む。

### 実装メモ

- **プロバイダの組み立てを `ui/app.py` に置いた**（2026-08-07 承認）。tasks.md は
  `config.py` を想定していたが、`ors/client.py` が `config.py` の定数
  （`RATE_LIMIT_RETRY_WAIT_S` / `REQUEST_TIMEOUT_S`）を import しているため、
  `config.py` から `ors/client.py` を import すると**循環 import** になる
  （`config → ors.client → config`）。依存の向きが一方向の `ui/` は
  どちらも安全に import できる。**design.md 1.3 の依存図と理由を直した。**
  `ports.py` の Protocol と6例外だけを上位の語彙にする方針（3.1）は変わらない
- **`[build-system]`（hatchling）を追加し、`runloop` / `ui` を editable install
  される形にした**（2026-08-07 承認）。`streamlit run ui/app.py` は
  **スクリプト自身のディレクトリ（`ui/`）しか `sys.path` に足さない**ので、
  パッケージとして入っていないと `import runloop` が `ModuleNotFoundError` に
  なる。pytest の `pythonpath = ["."]` は pytest 専用の設定で、Streamlit の
  起動には効かない。**手動確認で初めて現れる種類の不具合**で、
  自動テストは全件通ったままだった
- **`st.secrets` は参照しただけでは例外を出さない**（2026-08-07 に実測）。
  遅延評価のプロキシで、実際に読む時点（`in` / `[]`）で初めて
  `StreamlitSecretNotFoundError` を送出する。最初の実装は
  `env = st.secrets` を `try` で囲っており、例外は `load_settings()` の
  内側（`try` の外）で出ていた。`load_settings(st.secrets)` ごと `try` で
  囲む形に直した。**`secrets.toml` はあるがキーが無い場合は `.env` に
  落ちない**（`ApiKeyMissing` をそのまま伝える）——「渡されたときは
  `os.environ` に触れない」という `load_settings` の契約（design.md 3.3）に合わせる
- **「実行ボタンを無効にする」（AC-07-4 の完了条件）を、この時点で唯一の
  操作である「この地点を起点にする」ボタンに読み替えた。** 目標距離を
  入れて実行するボタンは T18a の成果物でまだ無い。`PENDING` の間は
  結論を出さないという規律（design.md 8.4）は、いま存在する確定操作に対して
  同じように効かせている。**T18a で実行ボタンを足すときに同じ無効化を配る**
- **確定した回の描画に `load()` の結果を使っていない**（design.md 8.4 の
  T17 への申し送り）。`save()` は再実行を起こさず、書き込み直後の `load()` は
  保存前の値を返す。確定処理を地図の描画より**前**に済ませ、確定した値
  そのものを `build_map()` に渡している（`st.empty()` で地図の位置だけ
  先に確保する構造）。読み直すと1手ぶん古い起点が出て、
  利用者にはクリックが効かなかったように見える
- **クリックの重複を「前回見たクリック」との比較で防いだ。** `st_folium` は
  確定後も同じ `last_clicked` を返し続けるので、これが無いと確定した起点が
  即座にまた未確定の候補として現れる（確定ボタンが消えない）
- **このタスクには自動テストが無い**（design.md 10.4「Streamlit 画面は手動確認」）。
  したがって**スタブで赤を確認する手順と故意破壊の手順は踏んでいない。**
  代わりに実機で6つの AC を確認した（上記）。**T12〜T16 と違い、
  ここで見つかった不具合2件はどちらも自動テストが全件緑のまま起きた**もので、
  この層を手動で確認する方針が実際に効いた事例として記録しておく

### 参照

design.md 1.3 / 4.6.1 / 4.6.3 / 8.2 / 8.4 / 9.1 / 10.2 / 10.4、requirements.md AC-01-3 / AC-05-1 / AC-05-3 / AC-06-2 / AC-07-2〜4

### ブランチ

`feat/app-origin`（完了、main へ ff マージ済み）

---

## T18a: ui/app.py — 実行と結果表示

状態: 完了

### 目的

目標距離を入れて実行し、コースと距離とチェックポイントが出るところまで。
**このタスク完了時に一度、動くアプリとして手動確認する。**

### 触るファイル

`ui/app.py`

### 依存

T17, T13, T09, T11

### 完了条件

- [x] AC-01-1「起点と目標距離を指定して実行すると、コースが1本地図上に表示される」
- [x] AC-02-1「合計距離がキロメートル単位（小数第2位まで）で表示される」→ 実機で 5.11 km
- [x] AC-02-2「距離誤差（合計距離 − 目標距離）がメートル単位で、符号付きで表示される」→ 実機で +109 m と -279 m の両方
- [x] AC-02-3「合計距離が目標より短い場合、不足分を起点付近で調整する案内が表示される」→ 誤差 -279 m の回で確認
- [x] AC-02-4「合計距離が目標より長い場合、超過分を手前で切り上げる案内が表示される」→ 誤差 +109 m の回で確認
- [x] AC-03-3「選ばれたコースの獲得標高が画面に表示される」（ループ区間のみの注記つき）→ 実機で 54 m
- [x] AC-04-1「チェックポイントが最大5件表示される」→ 実機で5件（0.8 / 1.7 / 2.4 / 3.3 / 4.3 km）
- [x] 状態は `st.session_state["run"]` に `RunSession` を1つ置くだけ（design.md 6.1）
- [x] 表示する文言はすべて `messages.py` から取る
- [x] 非機能要件「実行から結果表示までを10秒以内に完了する」を手元で1回実測した → **3.6 秒**（経路A）
- [ ] AC-02-5「接近距離が 50m を超える場合、その値を画面に表示し、ずれの向きも併せて伝える」
      → **T19 に持ち越し。** 実装は入っているが、実機で使った起点は道路に近く
      50m 以下だったため文言が出る条件に入らなかった（出ないことが正しい挙動）。
      **道路から 50〜300m 離れた起点を選んで確かめる必要がある**
- [ ] AC-01-4 の妥協パスの表示（誤差最小の1本＋「条件を満たすコースが見つかりませんでした」）
      → **T19 に持ち越し。** 実行では15本すべてが ±300m を満たし `IN_TOLERANCE` に
      なったため、妥協パスを通っていない。**極端な目標距離で条件を作る**
- [ ] **T16 → T17 → T18a と持ち越してきた**（2026-08-07）: 非機能要件
      「スマートフォンの縦画面で、指で操作できる大きさの部品を用いる」
      → **T19 に持ち越し。** T18a の実機確認では見ていない。
      見る対象は地図のチェックポイントの丸印・起点マーカー・実行ボタン・距離の入力欄
- [ ] **T17 の申し送り**: 実行ボタンにも `PENDING` の間の無効化を配る
      （design.md 8.4）→ **実装済み**（`search_disabled` に
      `load.state is LoadState.PENDING` を含めた）。画面での確認は
      `PENDING` が一瞬しか続かないため取れていない

### 実機での実測（2026-08-07）

**動くアプリでの初めての実測。** 詳細は `spike/FINDINGS.md` スパイク7 に記録した。

| 観測 | 値 | 要件 |
|---|---|---|
| 所要時間 | **3.6 秒** | 10秒以内 → 満たす |
| API 消費 | **15 回** | 15回以内 → 満たす（ちょうど） |
| 取得できた候補 | **15 / 15 本** | 失敗 0 件 |
| 合計距離 / 誤差 | **5.11 km / +109 m** | ±300m → 満たす |
| チェックポイント | **5 件**（0.8 / 1.7 / 2.4 / 3.3 / 4.3 km） | AC-04-1 / AC-04-3 |
| 地点名 | **0 件** | AC-04-4（T05 の実測と整合） |

**経路 A の所要は推計（約2秒）より遅い 3.6 秒だった**が、要件には余裕がある。
**Community Cloud での実測は別**（design.md 11節 #10 は閉じない）。
**429 と経路 B の所要は未実測**（#16 も閉じない）。

### 実装中に見つかった配線の欠落（2026-08-07）

**`Candidate.turns` を誰も埋めていなかった。** T11 でフィールドと
`extract_turns` を作ったが、`generation._to_candidate` が
`ProviderRoute.steps` を捨てていたため、`ui/` に届く候補の `turns` は
常に空だった。**チェックポイント（AC-04-1）が1件も出せない状態**で、
自動テストは全件緑のままだった（T11 のテストは `extract_turns` を
直接呼び、T08/T09 のテストは `turns` を見ていなかった）。
`_to_candidate` で `extract_turns(route.steps)` を呼ぶ形にし、
`tests/test_generation.py` に3件のテストを足した。

**AC-04-4 の標準形はキロメートルだった。** T16 の吹き出しはメートル
（「起点から1234m 左折」）で出していたが、requirements.md AC-04-4 は
**文面そのもの**を「起点から 1.2km 地点を左折」と定めている。
文言を `messages.checkpoint_line()` に集約し（地図の吹き出しと
一覧の2か所が同じ定義元を使う）、`ui/map_view.py` の翻訳表を
そちらへ移した。T16 のテストも標準形に合わせて直した。

### この時点で残る未実装（T18b で閉じる）

失敗経路の分岐（AC-06-1〜3 の表示）と予期しない例外の捕捉（AC-06-4）は T18b。
**T18a の段階では API 失敗時に Streamlit のトレースバックが画面に出る。**
これは中間状態として受け入れる。`NO_CANDIDATE` / `ORIGIN_REJECTED` の
ときに結果欄が空になるのも同じ扱い（文言は AC-06-3 の担当）。

### 実機確認で出た2件（2026-08-07）

#### 1. `AttributeError: module 'runloop.messages' has no attribute 'checkpoint_line'`

**コードの欠落ではなかった。** 原因は
**Streamlit がメインスクリプトだけを読み直し、インポート済みのモジュールを
再読み込みしないこと**である。起動しっぱなしのサーバーで確認したため、
`ui/app.py` は最新（`checkpoint_line` を呼ぶ）に更新される一方、
`runloop.messages` は起動時点の版（`checkpoint_line` が無い）が
`sys.modules` に残り続けていた。

**再現して確定させた。** `checkpoint_line` を消した状態でサーバーを起動 →
ファイルを元に戻す → ページを再読み込み、という手順で、
`app.py` に一時的に置いた診断行が「app.py は今読まれた」と表示しながら
`hasattr(messages, "checkpoint_line")` が `False` を返した。
**片方だけが更新されることが目で見える形になった。**

| 何が | 再実行で読み直されるか |
|---|---|
| `streamlit run` に渡したスクリプト（`ui/app.py`） | **される** |
| `import` したモジュール（`runloop/**`、`ui/map_view.py`） | **されない** |

**対処は「`runloop/` を触ったらサーバーを再起動する」。**
`--server.runOnSave` を有効にしても、再読み込みされるのはスクリプトだけで
この問題は解けない。**T19 の確認手順にも入れる。**

**2回目の再発で、本当の原因が分かった（2026-08-07）。**
「再起動を忘れた」のではなく、**再起動したつもりで古いプロセスが生き残り、
ブラウザがそちらに繋がっていた。** 開発中に使っていた
`pkill -f "streamlit run ui/app.py"` が**一度も効いていなかった**ためで、
Windows での実際のコマンドラインが
`...\streamlit.exe run ui/app.py` の形（`streamlit.exe run`）なので、
`"streamlit run ui/app.py"` というパターンに一致しない。
**気づいた時点で 32 個のプロセスが積み上がっており**、ポート 8501 は
最初の1個が掴んだままだった。新しく起動したプロセスも
「Local URL: http://localhost:8501」と表示するので、**ログを見ても
どれに繋がっているか判別できない。**

Windows で確実に止める方法。

```powershell
Get-CimInstance Win32_Process -Filter "Name like '%python%'" |
  Where-Object { $_.CommandLine -like '*streamlit*' } |
  ForEach-Object { Stop-Process -Id $_.ProcessId -Force }
```

**「行番号が変わったから app.py は新しい ＝ 再起動できている」は成り立たない。**
古いプロセスでもメインスクリプトは毎回読み直されるので、行番号だけは
最新になる。この推論で2回とも「再起動はできている」と判断してしまった。

**手順ではなく構造で防ぐことにした。** `runloop/__init__.py` に
`LOADED_AT`（パッケージが import された時刻）を置き、`ui/app.py` の先頭で
`runloop/**` と `ui/**` のソースの更新時刻と比べる。ソースのほうが新しければ
**`st.error` で「サーバーが古いコードで動いています」と出して `st.stop()`**
する。`AttributeError` のトレースバックではなく、何をすればよいかが画面に出る。

**検出器が検出したい状態で壊れる罠を踏んだ。** 最初の実装は
`runloop.LOADED_AT` を直に読んでいたので、この仕組みを入れる前の版が
載ったプロセスでは
`AttributeError: module 'runloop' has no attribute 'LOADED_AT'` になった。
`getattr` で受けて、無ければ「確実に古い」と結論する形に直した。
**両方向（クリーンなら出ない・`touch` すれば出る）を実機で確認した。**

**自動テストと mypy は正しかった。** どちらも欠落を見逃していない——
そもそも欠落していないので、検出すべきものが無かった。
念のため `messages.checkpoint_line_MISSING(...)` に書き換えて確かめたところ、
mypy は `ui/app.py:221: error: Module has no attribute
"checkpoint_line_MISSING"; maybe "checkpoint_line"? [attr-defined]` を出した。
**`ui/app.py` は mypy の対象に入っており、「存在しない関数を呼ぶ」欠落は
`uv run mypy .` で検出できる**（画面の自動テストが無くても、この種の
欠落は型検査が捕まえる）。CI にも入っているので push すれば止まる。

#### 2. 地図をドラッグすると初期位置に戻る

`st_folium` は既定で地図の**移動・拡大の結果も**返し、そのたびに
Streamlit が再実行される。再実行のたびに `build_map()` が
初期の中心・倍率で新しい地図を組み立て直すため、動かした位置が失われていた。

`returned_objects=["last_clicked"]` を渡して**クリックだけを受け取る**ようにした。
パンとズームでは再実行が起きなくなり、操作が保たれる。起点の指定
（AC-05-1）はクリックで動くので影響しない。

**併せて `fit_bounds` を足した**（`ui/map_view.py`）。起点を中心に固定倍率で
描いていたので、目標距離が大きいコースは画面からはみ出していた。
コースがあるときだけ、起点とコース全体が入る範囲に寄せる（1点だけに
寄せると最大倍率まで寄ってしまうので、コースが無いときは呼ばない）。

### 申し送り: キャッシュ乖離の破棄が永続しない

`generation` が `cache_diverged` を返したとき（design.md 8.5.1）、
**保存済みのスナップ距離だけを捨てる手段が `OriginStore` に無い。**
`clear()` は起点ごと消すため、8.5 の「起点は残す」に反する。
T18a では**そのセッションの間だけ**キャッシュを使わないようにして
経路 B に落としている（`st.session_state["cache_diverged"]`）。
ページを再読み込みすると保存された値がまた使われ、乖離が再検出される。
実害は「その1回の実行で経路 A を通る」ことに限られ、表示と判定は
どちらも実測から出るので AC-01-2 / AC-01-3 は守られる（design.md 4.6.3）。
恒久対応は `OriginStore` にメソッドを1つ足すことになるので、T18b か
T19 で判断する。

### 参照

design.md 6.1 / 9.1 / 12節、requirements.md AC-01-1 / AC-01-4 / AC-02-1〜5 / AC-03-3 / AC-04-1 / 7節（性能）

### ブランチ

`feat/app-run`

---

## T18b: ui/app.py — 引き直しと例外処理

状態: 完了

### 目的

引き直しを配線し、どの経路でもアプリが止まらない状態にする。

### 触るファイル

`ui/app.py`

### 依存

T18a

### 完了条件

- [x] AC-08-1「引き直しを操作すると、次に良い候補が表示される」→ 実機で 15 本すべてを引き直し、押すたびにコースが変わることを確認
- [x] AC-08-2「引き直しでは外部 API を呼び出さない」→ 実機で「実行の記録」の API 消費が **15 回のまま増えない**ことを確認
- [x] AC-08-3「在庫を出し切った場合、それ以上の候補がない旨を表示する」＋「もう一度探す」を**同じ画面に置く**（design.md 6.3）→ 実機で確認。**押す前から**出す形にした（下記）
- [x] AC-06-1「API 呼び出しが失敗した場合、失敗した旨のメッセージが表示される」→ 接続不能を再現して「地図サービスに接続できませんでした」が出ることを確認（**API 消費ゼロ**）
- [x] AC-06-3「候補が1件も取得できなかった場合、その旨のメッセージが表示される」（全滅・異常値除外で0件の両方）
- [x] AC-06-4「いずれの異常時も、アプリが停止せず再実行できる状態を保つ」→ `RouteProviderError` とその他の例外をそれぞれ捕まえ、**トレースバックを画面に出さない**。入力（起点・目標距離）を保持する
- [x] 例外の型・ステータス・残数・失敗の内訳はログへ。画面には行動可能な文言だけ（design.md 9.2）
- [x] AC-08-3 のメッセージを例外処理ではなく**通常の経路**として作っている（悲観側では2本以上が81.4%。design.md 6.3）

### 実機での実測（2026-08-07）

所要 **3.4 秒** ／ API 消費 **15 回** ／ 候補 **15 本** ／ 経路A。
**±300m を満たした在庫は 15/15 本**で、T18a と同じ結果だった。
引き直しでは API 消費が増えないことを画面で確認した（AC-08-2）。
詳細は `spike/FINDINGS.md` スパイク8。

**design.md 6.3 の悲観側の見積もり（2本以上が 81.4%、期待在庫 2.9 本）は、
5km・この起点では悲観的すぎた**（2回とも 15/15）。一方 10km では在庫が
減っており、要検証 #1 の材料として design.md に記録した。

### 実装中に見つかったこと（2026-08-07）

#### design.md 9.1 の対応表に AC-08-3 の行が無かった

そのため `messages.py`（T12）にも文言が無く、**在庫を出し切ったときに
何も言えない状態だった。** T12 の完了条件は「9.1 の対応表を網羅している」で、
テストも表を写して網羅を検査していたので、**表自体の抜けはそのまま通り抜けた。**
網羅の基準を表に置く以上、表の抜けは検査で見つからない。
表と `messages.stock_exhausted()` の両方を足した。

#### 全滅の原因が画面に伝わっていなかった（AC-06-1）

接続先を存在しないホストに向けて確かめたところ（**API 消費ゼロ**）、
15本すべてが接続不能で失敗したのに画面には AC-06-3 の
「候補が得られませんでした。**起点を道路の近くに指定し直すか**」しか出ず、
**起点は悪くないのに起点を疑わせる**案内になっていた。
ログには `ProviderUnavailable 15件` と出ており、情報はあったが
`RunSession` が運んでいなかった。`failures` を持たせ、
`messages.failure_summary()` で最も多い種類から文言を引くようにした。
修正後は「地図サービスに接続できませんでした。通信の状態を確かめて、
もう一度実行してください。」が出る（トレースバックは出ない。AC-06-4）。

#### ボタンの活性が1手遅れていた

Streamlit のボタンは**描画した時点の状態**で活性が決まる。「探す」を押した
回では、ボタンを描く時点の `run` はまだ実行前（初回は `None`）なので、
**在庫が何本あっても「別のコースを見る」が無効のまま描かれていた。**
実機で「押せない」と報告されたのはこれで、在庫が1本だったからではない。
実行・引き直しの後に `st.rerun()` を入れて、活性を状態に追いつかせた
（`localStorage` への書き込みは挟まないので design.md 8.4 の
「`setItem` の直後に `st.rerun()` を呼ばない」には触れない）。

#### 押せない理由を画面に出すようにした

ボタンが灰色なだけでは、なぜ押せないのかが分からない（design.md 9.2）。
在庫の末尾にいるときは**押す前から**「これ以上の候補がありません」を出す。
在庫1本の実行は最初からこの状態で、尽きること自体は通常の経路である
（design.md 6.3）。ボタンの活性判定（`_can_reroll`）は `session.reroll` と
同じ「次の1本があるか」で決め、2か所の判定が食い違わないようにした。

### 参照

design.md 6.2 / 6.3 / 9.1 / 9.2、requirements.md AC-06-1 / AC-06-3 / AC-06-4 / AC-08-1〜3

### ブランチ

`feat/app-reroll-errors`

---

## T19: 結合確認（受け入基準の全件チェックと異常系）

状態: 進行中

### 目的

受け入基準を上から順に実画面で確認し、性能と API 消費の前提が実際に成立していることを
確かめる。

### 依存

T18b

### 完了条件

- [ ] AC-01-1〜AC-08-4 の全件を手動チェックリストで通した（WORKFLOW.md フェーズ6）
      - [x] **第2段（起点まわり、API 2回）完了**（2026-08-07、第3段の前）。
            桜島の山中 → 「道路が見つかりません」で確定されない。
            自宅付近 → 確定してマーカーが出る（AC-05-1 / AC-05-2 / AC-07-1）
      - [x] **第3段（極端な値、API 30回）完了**。FINDINGS スパイク9 を参照
      - [x] **第4段（5km、API 15回）完了**。候補 **15/15 本**（3回目も同じ。
            5km では安定）。所要 8.9 秒（上記）
      - [x] **第1段（キー未設定・AC-07-4、API 0回）完了**。
            AC-06-2 の案内とボタンの無効化、`localStorage` を消してリロードして
            AC-07-4 の案内とマーカー無しを、どちらも画面で確認
      - [ ] 第5段（スマホでの操作性、0回）
- [x] 非機能要件「実行から結果表示までを10秒以内に完了する」を実測した
      → **3回とも満たしたが、ばらつきが大きい**（T18a 3.6秒 / T18b 3.4秒 /
      **T19 第4段 8.9秒**）。条件は同じ（候補15本・消費15回・経路A）。
      8.9 秒では**余裕が 1.1 秒**しかない。直前に30回消費した後の実行で、
      ORS 側が毎分制限に近い状態だった可能性（n=1、切り分けできていない）。
      **要検証 #17 を新設した**——8.9 秒は最遅の1本がタイムアウト（8.0秒）
      直前だったことを示唆し、**タイムアウト+投げ直しで 16 秒**になりうる
      という設計の内部矛盾が見つかった。詳細は FINDINGS スパイク10
      - [ ] Community Cloud での実測（要検証 #10）はデプロイ後。**重要度が上がった**
- [ ] 非機能要件「1回の実行における外部 API 呼び出しを 15 回以内に抑える」を、残数ヘッダの推移で確認した
- [x] 非機能要件「毎分の呼び出しを 40 回未満に抑える」を、連続実行で確認した
      → **T19 第3段で 30 回を短時間に消費したが 429 は出なかった。**
      1回の実行が15回なので「1分以内に2回までなら安全」という前提
      （requirements.md 7.1）が実測と整合した
- [ ] **429 を引いたときの経路Bが10秒に収まるか**を測った（design.md 11節 #16）。
      並列の中の待機は重なるので積み上がらないが、二段が直列なので両段で 429 を
      引くと約 12.6 秒と推計される（T08 で確認。推計であり実測ではない）
- [x] 極端な値（0.1km / 100km）が適切に扱われる → **T19 第3段で確認**。
      0.1km は誤差 -8m / +3m で在庫あり（精度は予想外に良い）、
      100km は在庫0本で妥協パス。**距離が伸びるほど誤差が広がる**ことが
      0.1 / 5 / 10 / 100km の4点で一貫した（要検証 #1 に記録）
      - [x] 道路から離れた地点のクリック → **T19 第2段で確認**（第3段の前に実施）。
            **桜島の山中で「この地点の近くに道路が見つかりません。最寄りの道路上を
            クリックしてください。」が出て確定されなかった。** これは AC-01-3 の
            **距離を言わない変種**（`snap` が `null`。design.md 4.6.1）で、
            T17 で確認済みの**距離つきの変種**（「起点が道路から ◯m 離れています」）と
            対になる。**2つの変種が両方とも実機で確認できた**——
            観測していない距離を画面に出さない設計（T07 / T08 の「0 で埋めない」と
            同じ判断）が成立している
- [ ] `/code-review` と `/security-review` を流し、指摘に対応したか対応しない理由が説明できる（WORKFLOW.md フェーズ5）
      - [x] **`/security-review` 実施済み（2026-08-07）。XSS を1件見つけて修正した。**
            `ui/map_view.py` がチェックポイントの吹き出しを folium に生のまま渡しており、
            **道の名前（OpenStreetMap 由来で誰でも編集できる）で JavaScript を実行できた。**
            folium の `Tooltip` は値をエスケープせず **JS のテンプレートリテラルの内側**に
            差し込むため、**HTML エスケープだけでは防げない**（`${...}` が評価される）。
            被害は localStorage の**丸めていない自宅座標**の流出で、design.md 8.7 が
            守ろうとしているものがこの経路で破られていた。
            `_safe_tooltip()` で HTML 実体参照に置き換えて修正し、回帰テストを3件足した
            （故意破壊3パターンで検出を確認）。判断は [ADR-0005](adr/0005-escape-provider-strings-at-the-view.md)、
            経緯は `spike/FINDINGS.md`、方針は design.md 8.7 に記録した。
            **デプロイ前に見つかったので、穴が開いた状態で公開されたことはない**
      - [ ] `/code-review` は未実施
- [ ] 設計から逸脱した点があれば `docs/design.md` を更新した
- [x] **T17 からの持ち越し**（2026-08-07 承認）: AC-06-2「API キーが未設定の
      場合、設定が必要である旨のメッセージが表示される」
      → **T19 第1段で確認**（API 0回）。案内が出て、**ボタンが全部無効**になった
      （送信する経路に到達しない）
- [ ] **T18a からの持ち越し**（2026-08-07）: AC-02-5「接近距離が 50m を
      超える場合、その値と**ずれの向き**を表示する」。T18a の実機では
      起点が道路に近く（50m 以下）条件に入らなかった。
      **道路から 50〜300m 離れた地点を起点にして確かめる**
      → **T19 でも未確認のまま。** そういう地点の心当たりが無く、
      第2段で試した桜島の山中は 350m 以内に道路が無い側（`snap` が `null`）
      だった。**未検証項目として残す**（実装とテストは入っている）
- [x] **T18a からの持ち越し**（2026-08-07）: AC-01-4 の妥協パス
      → **100km で確認できた**（T19 第3段）。「条件（目標距離との差 ±300 m 以内）を
      満たすコースが見つかりませんでした。最も近い1本を表示しています」＋
      合計距離 99.54 km（誤差 -461 m）。**同時に AC-08-4 も確認**——
      妥協パスでは在庫が空なので「別のコースを見る」が無効になり、
      ±300m 未満の候補で在庫を埋めない設計が画面上で成立していた
- [ ] **T16 → T17 → T18a と持ち越してきた**（2026-08-07）: 非機能要件
      「スマートフォンの縦画面で、指で操作できる大きさの部品を用いる」。
      地図のチェックポイントの丸印・起点マーカー・実行ボタン・距離の入力欄を、
      実機のスマートフォンの縦画面で確かめる
- [ ] **AC-07-4 を見るときは先に localStorage を消す**（T17 の実機確認での
      取り違え）。保存が残っている状態が既定なので、消さずに見ると
      AC-07-2（復元）を見ていることになる。
      `localStorage.removeItem("runloop.origin.v1")` の後にリロードする
- [ ] **`runloop/` を触ったらサーバーを再起動してから確認する**（T18a の
      実機確認で `AttributeError` を出した原因）。Streamlit が読み直すのは
      `ui/app.py` だけで、`import` したモジュールは `sys.modules` に
      残り続ける。**画面だけが新しく、中身が古い**状態になり、
      実在する関数が「無い」というエラーになる。
      **`ui/app.py` が起動時に検出して止めるようにした**（T18a）ので、
      「サーバーが古いコードで動いています」が出たら再起動する。
      Windows では `pkill` が効かない（コマンドラインが `streamlit.exe run`
      の形）。上記の PowerShell で確実に止める

### 参照

WORKFLOW.md フェーズ5・6、design.md 11節、requirements.md 5節・7節

### ブランチ

`test/integration-check`
