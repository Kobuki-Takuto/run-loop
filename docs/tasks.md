# 実装タスク一覧（フェーズ3 の成果物）

- 版: 1.0
- 作成日: 2026-08-05
- 根拠: [requirements.md](requirements.md)（版1.2・確定）と [design.md](design.md)（版1.1）
- 進め方: [WORKFLOW.md](WORKFLOW.md) フェーズ4（1タスク = 1ブランチ = 1PR、半日以内）

**状態はこのファイルだけで管理する。** 各タスクの先頭の `状態:` を書き換えて進める。
一覧表に状態を持たせないのは、2か所に書くと必ず食い違うため。

| 状態 | 意味 |
|---|---|
| 未着手 | まだ手を付けていない |
| 進行中 | ブランチを切って作業中 |
| 完了 | PR をマージし、完了条件をすべて満たした |

## 着手前に決めたこと（2026-08-05 承認）

1. `streamlit-js-eval` を追加してよい（T15 の直前で追加する）
2. T05（実データ取得スパイク）を実施する。API 約3回の消費を払う
3. CI（GitHub Actions）を T01 で入れる

## 8月12日ごろに確認すること

要検証 #3 のうち「iOS が未使用のサイトデータを消すか」は未実施（スパイク5から数時間しか
経っていないため）。**永続化の実装（T14 / T15）はこの結果に依存しない形に割ってある。**
方式が変わった場合の差し替え範囲は T15 だけで、T14 のポートと検証ロジックは変わらない。

---

## 一覧（依存順）

| # | タスク | ブランチ | 依存 |
|---|---|---|---|
| T01 | パッケージ骨格と CI | `chore/skeleton-ci` | — |
| T02 | models.py と geo.py | `feat/models-geo` | T01 |
| T03 | ports.py | `feat/ports` | T02 |
| T04 | config.py | `feat/config` | T03 |
| T05 | 実データ取得と fixture 化（**API 約3回**） | `spike/fixture-points3` | T01 |
| T06 | ors/mapper.py | `feat/ors-mapper` | T05, T03 |
| T07 | ors/client.py | `feat/ors-client` | T06, T04 |
| T08 | generation.py 二段投入（経路B） | `feat/generation-two-stage` | T07 |
| T09 | generation.py 一括並列（経路A）と乖離検出 | `feat/generation-cached-path` | T08 |
| T10 | selection.py | `feat/selection` | T02 |
| T11 | checkpoints.py | `feat/checkpoints` | T06, T02 |
| T12 | messages.py | `feat/messages` | T02 |
| T13 | session.py | `feat/session` | T10 |
| T14 | persistence.py ポートと純ロジック | `feat/persistence-core` | T02 |
| T15 | localStorage 実装（`streamlit-js-eval`） | `feat/persistence-localstorage` | T14 |
| T16 | ui/map_view.py | `feat/map-view` | T02 |
| T17 | ui/app.py 起点の確定フロー | `feat/app-origin` | T15, T16, T12, T07 |
| T18a | ui/app.py 実行と結果表示 | `feat/app-run` | T17, T13, T09, T11 |
| T18b | ui/app.py 引き直しと例外処理 | `feat/app-reroll-errors` | T18a |
| T19 | 結合確認 | `test/integration-check` | T18b |

T10〜T14 は互いに独立で、T02 が終われば並行できる（T10 は T08 の `GenerationOutcome` の
型だけ共有する）。**T18a が終わった時点で「コースが出るアプリ」になる。**

## 共通の完了条件（全タスク）

- [ ] `uv run pytest` / `uv run ruff check .` / `uv run mypy .` が通る（design.md 10.5）
- [ ] 差分を自分で読んで理解している（WORKFLOW.md フェーズ4）
- [ ] 1タスク = 1ブランチ = 1PR、半日以内

---

## T01: パッケージ骨格と CI

状態: 進行中（手元の3コマンドは通った。**CI が緑になることは push 後に確認**）

### 目的

`runloop/` と `ui/` の空構造を作り、検証コマンド3点と CI を通す土台を置く。

### 触るファイル

`runloop/__init__.py`, `ui/__init__.py`, `tests/`, `pyproject.toml`（ruff / mypy / pytest 設定）,
`.github/workflows/ci.yml`, `main.py`（雛形を撤去）

### 依存

なし（最初のタスク）

### 完了条件

- [x] `uv run pytest` / `uv run ruff check .` / `uv run mypy .` の3つが通る（design.md 10.5「各タスクの完了条件とする」）
- [ ] push で CI（ruff + mypy + pytest）が走り、緑になる（WORKFLOW.md フェーズ6 の雛形）← push 待ち
- [x] 非機能要件「API キーをソースコードおよびリポジトリに含めない」を満たす（`.env` が git 管理外であることを再確認。`git ls-files .env` が空）
- [x] `ui/` 以外に `import streamlit` が無い（design.md 1.2 の規律。lint で守れる形にする）
  - ruff の `flake8-tidy-imports` で `streamlit` / `streamlit_folium` を禁止し、`ui/**` だけ除外
  - `tests/test_layering.py` でも二重に固定（設定を緩めたときに静かに崩れないように）

### 実装メモ

- `main.py`（`uv init` の雛形）を削除した。Streamlit の入口は T17 以降の `ui/app.py`
- `spike/**` は ruff の `ANN` / `E501` / `TID251` を除外した。**`E501` を外したのは、
  design.md 3.3 が spike のコードを行番号つきで引用しており、整形で行がずれると
  引用が合わなくなるため**
- mypy は `strict = true`、`spike/` は対象外（使い捨てで本体から参照しない）
- pytest に `pythonpath = ["."]` を入れた（`runloop` / `ui` をインストールせずに import する）
- テスト関数にも `-> None` を書く（mypy strict と CLAUDE.md「型ヒントを必ず書く」に合わせ、
  ruff 側の例外は置かない）

### 参照

design.md 1.1 / 1.2 / 10.5、WORKFLOW.md フェーズ0・6

### ブランチ

`chore/skeleton-ci`

---

## T02: models.py と geo.py（合計距離を計算プロパティにする）

状態: 未着手

### 目的

データ構造と座標計算を置く。合計距離は**計算プロパティ**とし、判定値と表示値が同一であることを
構造で保証する。

### 触るファイル

`runloop/models.py`, `runloop/geo.py`, `tests/test_models.py`, `tests/test_geo.py`

### 依存

T01

### 完了条件

- [ ] AC-01-2「表示されたコースの**合計距離**（ループ距離 + 接近距離 × 2）が、目標距離の ±300m 以内である」→ `total_m` と `is_within_tolerance` のテスト
- [ ] AC-01-5「合計距離が目標距離の3倍を超える候補は異常値として除外する」→ `is_degenerate` のテスト
- [ ] AC-02-2「距離誤差（合計距離 − 目標距離）がメートル単位で、符号付きで表示される」→ `error_m` の符号のテスト
- [ ] 境界値4件が design.md 5.1 の表どおり（誤差 300.0 は在庫に入れる／ちょうど3倍は異常値にしない／接近 50.0 は OK／300.0 は WARN）
- [ ] `geo.haversine` が既知の2点で期待値を返す（スパイクと同じ球近似）
- [ ] 丸めを一切しない（表示の丸めは T12 の責務。design.md 2.2）
- [ ] `frozen=True` のデータクラスで、`target_m` を `Candidate` に焼き付けている（design.md 2.2）
- [ ] テストを先に書き、失敗を確認してから実装した

### 参照

design.md 2.1 / 2.2 / 2.3 / 5.1、requirements.md AC-01-2 / AC-01-5 / AC-02-1 / AC-02-2

### ブランチ

`feat/models-geo`

---

## T03: ports.py（RouteProvider Protocol とドメイン例外）

状態: 未着手

### 目的

外部依存の境界を1か所に置く。上位層に `requests` と HTTP ステータスを持ち込まない。

### 触るファイル

`runloop/ports.py`, `tests/test_ports.py`

### 依存

T02

### 完了条件

- [ ] `RouteProvider` Protocol に `round_trip()` と `snap()` の2メソッドがある（design.md 3.1）
- [ ] design.md 3.2 の翻訳表の6例外（`ApiKeyMissing` / `ApiKeyRejected` / `RouteNotFound` / `RateLimited` / `ProviderUnavailable` / `MalformedRoute`）が定義され、基底 `RouteProviderError` を共有している
- [ ] フェイクのプロバイダが Protocol に適合し `mypy` が通る
- [ ] 異常ルート（合計距離が目標の3倍超）を例外にしていない。AC-01-5「除外した候補は AC-01-4 の妥協パスでも表示しない」は選択側（T10）の責務
- [ ] プロバイダが目標距離・±300m・3倍・接近距離の閾値を知らない（design.md 3.1「責務の境界」）

### 参照

design.md 3.1 / 3.2、requirements.md 非機能要件（保守性）

### ブランチ

`feat/ports`

---

## T04: config.py（定数と設定。フォールバック値を置かない）

状態: 未着手

### 目的

要件由来の数値を1か所に集め、設定の欠落で**静かに間違えない**ようにする。

### 触るファイル

`runloop/config.py`, `tests/test_config.py`

### 依存

T03

### 完了条件

- [ ] 定数（±300m / 15本 / 50m・300m / TTL 30日 / 乖離 10m / タイムアウト 8秒 / 送信上限 18回）が `config.py` にあり、コード中に散っていない
- [ ] AC-06-2「API キーが未設定の場合、設定が必要である旨のメッセージが表示される」→ キー欠落時に**1回も送信せず** `ApiKeyMissing`
- [ ] `os.getenv(...) or <定数>` および `.get(key, <それらしい既定値>)` の形が `runloop/` に無い（design.md 3.3。`spike/` のフォールバック座標を本体に持ち込まない）
- [ ] 起点を環境変数から読まない（起点はブラウザの永続化のみ。design.md 3.3 の表）
- [ ] 欠落時のメッセージにキー名と探した場所が入り、値そのものはログにも画面にも出ない

### 参照

design.md 3.3、requirements.md AC-06-2、非機能要件（セキュリティ）

### ブランチ

`feat/config`

---

## T05: 実データ1件の取得と fixture 化（API を約3回消費）

状態: 未着手

### 目的

points=3・`instructions: true` の実レスポンスと `/v2/snap` の実レスポンスを1件ずつ取得し、
要検証 #4 / #12 / #13 を埋める。

**API を約3回消費する**（directions 1回、`/v2/snap` 1〜2回）。2026-08-05 に承認済み。

### 触るファイル

`spike/`（使い捨てスクリプト）, `tests/fixtures/`（追加）, `docs/design.md` 11節, `spike/FINDINGS.md`

### 依存

T01（T02〜T04 と並行してよい）

### 完了条件

- [ ] `tests/fixtures/` に points=3・`instructions: true` の directions レスポンスが1件ある
- [ ] fixture に自宅座標が含まれていないことを確認済み（design.md 10.3 と同じ扱い）
- [ ] 要検証 #4（step 数・`name` が `"-"` の割合・maneuver の分布）に実測値が入る
- [ ] 要検証 #12（`/v2/snap` のスナップ先が directions と一致するか）に結論が入る
- [ ] 要検証 #13（`/v2/snap` が directions と同じ無料枠を消費するか）に応答ヘッダの残数で結論が入る
- [ ] 消費した呼び出し回数と残数の推移を FINDINGS に記録した
- [ ] スクリプトは `spike/` に置き、本体（`runloop/`）から参照しない

### 参照

design.md 7.3（要検証）/ 10.3 / 11節 #4・#12・#13

### ブランチ

`spike/fixture-points3`

---

## T06: ors/mapper.py（GeoJSON → ProviderRoute）

状態: 未着手

### 目的

ORS のレスポンス形式に依存する知識を `ors/` の中だけに閉じる。

### 触るファイル

`runloop/ors/mapper.py`, `tests/test_ors_mapper.py`

### 依存

T05（fixture）, T03

### 完了条件

- [ ] fixture の JSON から `ProviderRoute` が組める（ループ距離・`ascent`／`descent`・`snapped_start`・steps・3次元座標。design.md 10.2）
- [ ] AC-04-4「`"-"` は「名前なし」を意味する ORS の値であり、そのまま画面に出さない」→ `None` に正規化する。警告もログも出さない
- [ ] maneuver 種別を型に変換している（方向転換かどうかの判定は T11 のホワイトリスト）
- [ ] 想定外の JSON で `MalformedRoute` になる（design.md 10.2「壊れた JSON」）
- [ ] ORS 固有の知識（キー名・`"-"`・type 番号）が `ors/` の外に漏れていない
- [ ] テストを先に書き、失敗を確認してから実装した

### 参照

design.md 1.2 / 7.1 / 10.2、requirements.md AC-04-4

### ブランチ

`feat/ors-mapper`

---

## T07: ors/client.py（1回の呼び出しとステータスの翻訳）

状態: 未着手

### 目的

ORS への1回の呼び出しに責務を閉じる。並列化と本数の管理は T08 の仕事。

### 触るファイル

`runloop/ors/client.py`, `tests/test_ors_client.py`

### 依存

T06, T04

### 完了条件

- [ ] AC-03-1「生成されるコースに階段が含まれない」→ `avoid_features: [steps]` を必ず送る（`responses` で検証）
- [ ] AC-06-1「API 呼び出しが失敗した場合、失敗した旨のメッセージが表示される」の前提として、404→`RouteNotFound`、429→`RateLimited`、401/403→`ApiKeyRejected`、5xx／接続／タイムアウト→`ProviderUnavailable` に翻訳される
- [ ] リトライ回数のテスト: 429 は2回送信、404 は1回だけ（`responses` の呼び出し回数で検証）。**404 は無料枠を消費するため投げ直さない**（design.md 4.3 / requirements.md 4.6.1）
- [ ] 429 の待機は1秒。回復しなければ欠測として続行する（10秒の性能要件と衝突させない）
- [ ] タイムアウトが8秒で、応答待ちで固まらない
- [ ] `snap()` を半径 350 で投げ、`null`（半径内に道路なし）を区別して返す（design.md 4.6.1）
- [ ] `ratelimit_remaining` を `ProviderRoute` と例外が持ち回る。画面には出さずログに出す
- [ ] 例外メッセージにリクエストヘッダ（= API キー）を含めない（非機能要件・セキュリティ）
- [ ] 外部 API を実際に叩くテストを書いていない（`responses` でモック）

### 参照

design.md 3.2 / 4.3 / 4.5 / 4.6.1 / 10.2、requirements.md AC-03-1 / AC-06-1 / 4.6.1

### ブランチ

`feat/ors-client`

---

## T08: generation.py — 二段投入（経路B）と接近ゲート

状態: 未着手

### 目的

プローブ1本 → 接近ゲート → 残り14本を並列。道路から離れた起点で15回消費しないための
打ち切りを作る。

### 触るファイル

`runloop/generation.py`, `tests/test_generation.py`

### 依存

T07

### 完了条件

- [ ] AC-01-3「300m 超 → 結果を表示せず拒否する」の前段として、**プローブの応答が 300m 超のとき2本目以降が1回も送信されない**（design.md 10.2 が「テストの中心」と位置づけた項目）
- [ ] AC-06-4「いずれの異常時も、アプリが停止せず再実行できる状態を保つ」→ 15本中2本が 404 でも 13 本で選択が続く
- [ ] AC-06-3「候補が1件も取得できなかった場合、その旨のメッセージが表示される」→ 全滅（成功0本）で `NO_CANDIDATE` に落ちる
- [ ] 非機能要件「1回の実行における外部 API 呼び出しを **15 回**以内に抑える」→ 消費が15回を超えないテスト。**プローブの応答も候補として在庫に入れる**（別枠にすると16回になる）
- [ ] 接近距離は `geo.haversine(起点, snapped_start)` で generation が算出し、候補ごとに持たせる（design.md 2.2）
- [ ] 並列は `ThreadPoolExecutor`（実測済みの構成。`asyncio` に替えない）。フェイクのプロバイダで同時に投げていることを検証する（design.md 10.4）
- [ ] 失敗は種類別に数えて（`failures`）ログに出し、画面には出さない（design.md 4.4）
- [ ] シードは実行ごとに乱数で15個（重複なし）。固定しない（design.md 4.2）

### 参照

design.md 4.1 / 4.2 / 4.4 / 10.2 / 10.4、requirements.md AC-01-3 / AC-06-3 / AC-06-4 / 7節

### ブランチ

`feat/generation-two-stage`

---

## T09: generation.py — 一括並列（経路A）と乖離検出

状態: 未着手

### 目的

スナップ距離のキャッシュが有効なとき、接近ゲートを判定済みとして15本を一括で投げる
（節約するのは呼び出し数ではなく**時間**）。同時に、キャッシュが誤っていたことに
気づく手段を持つ。

### 触るファイル

`runloop/generation.py`, `tests/test_generation.py`

### 依存

T08

### 完了条件

- [ ] キャッシュが有効なとき、プローブを挟まず15本が同時に登録される（design.md 10.2「一括並列」／4.6.2 経路A）
- [ ] キャッシュがない／失効しているときは二段投入（経路B）に落ちる（同「経路の切り替え」）
- [ ] どちらの経路でも directions は15回（非機能要件「15 回以内」）
- [ ] 各候補の `approach_m` がキャッシュ値と **10m を超えて**離れていたら、キャッシュ破棄を呼び出し側に通知する（design.md 8.5.1）
- [ ] AC-01-2 の判定と表示には**各応答の `snapped_start` から算出した値**を使い、キャッシュ値を流用しない（design.md 2.2 / 8.5.1）
- [ ] 乖離を検出したらログに出す（座標は出さない）

### 参照

design.md 4.6.2 / 4.6.3 / 8.5.1 / 10.2

### ブランチ

`feat/generation-cached-path`

---

## T10: selection.py（6節の順序をそのまま実装する）

状態: 未着手

### 目的

本アプリの中核価値（距離の正確さ）を担う純関数。**距離を先に、標高を後に。**

### 触るファイル

`runloop/selection.py`, `tests/test_selection.py`

### 依存

T02（`GenerationOutcome` の型のみ T08 と共有）

### 完了条件

- [ ] AC-01-3 の拒否: `verdict == REJECT` のとき候補を1本も返さず `ORIGIN_REJECTED`（`NO_CANDIDATE` と区別する。design.md 5.2）
- [ ] AC-01-5「除外した候補は AC-01-4 の妥協パス（誤差最小の候補を出す経路）でも表示しない」→ 415km が在庫にも妥協パスにも出ない
- [ ] AC-03-2「距離条件（合計距離 ±300m）を満たす候補が2件以上ある場合、そのうち獲得標高が最小の候補が選ばれる」→ **標高が小さいが距離が外れた候補が選ばれない**ことのテスト（順序を逆にしていない）
- [ ] AC-01-4「±300m 以内の候補が得られなかった場合、距離誤差が最小の候補を表示し、条件を満たすコースがなかった旨のメッセージを表示する」→ `COMPROMISED`、在庫は空のまま
- [ ] AC-06-3「候補が1件も取得できなかった場合、その旨のメッセージが表示される」→ 異常値除外後に0件で `NO_CANDIDATE`
- [ ] AC-08-1「選択規準は初回表示と同一であり、ランダムではない」→ 並び順（獲得標高 昇順 → |距離誤差| 昇順 → seed 昇順）が決定的。標高が同値でも順序が揺れない
- [ ] 境界値（誤差ちょうど 300.0m は在庫、合計距離ちょうど3倍は異常値でない）がテストで固定されている
- [ ] 外部依存ゼロ（`requests` も Streamlit も import しない）
- [ ] テストを先に書き、失敗を確認してから実装した

### 参照

design.md 5.1 / 5.2 / 10.1、requirements.md 6節 / AC-01-2〜5 / AC-03-2 / AC-06-3 / AC-08-1

### ブランチ

`feat/selection`

---

## T11: checkpoints.py（方向転換の抽出と5件への間引き）

状態: 未着手

### 目的

AC-04 の4基準を1か所に集める。接近距離のオフセットを足す場所を型で固定する。

### 触るファイル

`runloop/checkpoints.py`, `tests/test_checkpoints.py`

### 依存

T06, T02

### 完了条件

- [ ] AC-04-1「チェックポイントが最大5件表示される」
- [ ] AC-04-2「各チェックポイントに、起点からの距離と方向転換の向きが表示される」→ `distance_from_origin_m = approach_m + cumulative_loop_m`。接近距離が**1回だけ**加算される（`Checkpoint` 生成時の1か所のみ。design.md 7.2）
- [ ] AC-04-3「方向転換が6か所以上ある場合、起点からの距離が均等になるよう5件に間引かれる」→ ループ長の6等分への最近傍で、結果が決定的
- [ ] AC-04-4「未観測の種別（13、ラウンドアバウト系）が来る前提とし、**知らない種別は方向転換として扱わない**側に倒す」→ ホワイトリスト（type 0〜5 のみ）。type 13 が方向転換にならないテスト
- [ ] AC-04-4「地点名は取得できた場合のみ併記する。名前がないことは異常として扱わない」→ `None` が画面文言に現れない
- [ ] 方向転換が5件以下ならすべて出す。0件でも例外にせず空を返す
- [ ] 間引きを通常の経路として実装している（実測では方向転換が76件あった。design.md 7.3）
- [ ] テストを先に書き、失敗を確認してから実装した

### 参照

design.md 7.1 / 7.2 / 7.3 / 10.1、requirements.md AC-04-1〜4

### ブランチ

`feat/checkpoints`

---

## T12: messages.py（画面文言を純関数で返す）

状態: 未着手

### 目的

文言の内容そのものが受け入基準なので、テストできる形（純関数）に集約する。
画面に文字列リテラルを書かない。

### 触るファイル

`runloop/messages.py`, `tests/test_messages.py`

### 依存

T02

### 完了条件

- [ ] AC-01-3「300m 超のメッセージは……**「最寄りの道路上をクリックしてください」と次にすべき操作を名指しする**」→ 文言に「最寄りの道路上」が含まれる
- [ ] 350m 以内に道路がない（`snap` が `null`）変種の文言に、距離が**含まれない**（design.md 4.6.1 / 10.1）
- [ ] AC-01-4 の「条件を満たすコースがなかった旨」の文言がある
- [ ] AC-02-1「合計距離がキロメートル単位（小数第2位まで）で表示される」→ 丸めはここだけで行う
- [ ] AC-02-2「距離誤差……がメートル単位で、符号付きで表示される」
- [ ] AC-02-3「不足分を起点付近で調整する案内が表示される」／AC-02-4「超過分を手前で切り上げる案内が表示される」→ 符号で切り替わる
- [ ] AC-02-5「接近距離が 50m を超える場合、その値を画面に表示し、**ずれの向きも併せて伝える**」→ 向きは「実走は表示より長くなる」。50m 以下では出さない
- [ ] AC-03-3「選ばれたコースの獲得標高が画面に表示される」→ **ループ区間のみの値である旨の注記**が付く
- [ ] AC-05-3 / AC-06-1 / AC-06-2 / AC-06-3 の文言があり、design.md 9.1 の対応表を網羅している
- [ ] すべての文言が次の行動を含む（design.md 9.2）
- [ ] 文言に座標・HTTP ステータス・API キーを含めない
- [ ] テストを先に書き、失敗を確認してから実装した

### 参照

design.md 9.1 / 9.2 / 10.1、requirements.md AC-01-3 / AC-01-4 / AC-02-1〜5 / AC-03-3 / AC-05-3 / AC-06-1〜3

### ブランチ

`feat/messages`

---

## T13: session.py（在庫と引き直しカーソル）

状態: 未着手

### 目的

US-08 の状態遷移を Streamlit なしでテストできる純データにする。

### 触るファイル

`runloop/session.py`, `tests/test_session.py`

### 依存

T10

### 完了条件

- [ ] AC-08-1「引き直しを操作すると、次に良い候補が表示される」→ カーソルが前進のみで進む（「前の候補に戻る」は実装しない）
- [ ] AC-08-2「引き直しでは外部 API を呼び出さない」→ 遷移が純データ操作で、プロバイダに触れない
- [ ] AC-08-3「在庫を出し切った場合、それ以上の候補がない旨を表示する」→ 末尾でカーソルを動かさない。**尽きたことを黙って同じコースを再表示しない**
- [ ] AC-08-4「在庫が尽きたときに ±300m 未満の候補で埋めることは**しない**」→ 妥協パス（`COMPROMISED`）では在庫が空
- [ ] 起点または目標距離が変わると在庫を破棄する（`query` を在庫の鍵に持つ。design.md 6.2）
- [ ] `RunSession` が1つのオブジェクトで、状態が複数キーに分かれていない（design.md 6.1）
- [ ] テストを先に書き、失敗を確認してから実装した

### 参照

design.md 6.1 / 6.2 / 6.3 / 10.1、requirements.md AC-08-1〜4

### ブランチ

`feat/session`

---

## T14: persistence.py — ポートと純ロジック（保存先に依存しない）

状態: 未着手

### 目的

`OriginStore` の境界と、「読めた値をどう信じるか」の判定を置く。
**この内容は保存先が変わっても変わらない**（8/12 のスパイク5の結果に依存しない）。

### 触るファイル

`runloop/persistence.py`, `tests/test_persistence.py`

### 依存

T02

### 完了条件

- [ ] AC-07-4「保存された起点がない初回起動時は、起点の指定を促す状態で表示される」→ 3状態の判定（未読→`PENDING`、キーなし→`EMPTY`、値あり→`LOADED`）。`load()` は `None` を返さない（design.md 8.4）
- [ ] AC-07-2「次回アプリを開いたとき、保存された起点が復元され地図上に表示される」→ **座標が妥当でスナップ距離だけ壊れているとき、起点は復元される**（design.md 8.6）
- [ ] design.md 8.6 の分類がそれぞれ破棄される（読めない／版が不明／欠けている／型が違う／範囲外／`snapped_distance_m > 300` の不変則違反／`probed_at` が不正または未来）
- [ ] `probed_at` が30日超のとき、スナップ距離のみ失効し**起点は残る**（design.md 8.5 条件3）
- [ ] 壊れた値は画面にエラーを出さず、ログに壊れ方の分類だけ出す。**ログに座標を出さない**（design.md 8.6 / 8.7）
- [ ] `save()` が `OriginRecord` を受け取り、座標とスナップ距離が**同時に**書かれる（別々に保存できる形にしない。design.md 8.3）
- [ ] `time.sleep()` で読み出しを待っていない（design.md 8.4）
- [ ] 座標を丸めていない（AC-05-1「自宅の玄関を正確に起点にしたい」）
- [ ] テストを先に書き、失敗を確認してから実装した

### 参照

design.md 8.2 / 8.3 / 8.4 / 8.5 / 8.6 / 8.7 / 10.1、requirements.md AC-07-1〜4

### ブランチ

`feat/persistence-core`

---

## T15: localStorage 実装（streamlit-js-eval）

状態: 未着手

### 目的

`OriginStore` の localStorage 実装。ブラウザ側の事情をここだけに閉じる。

**このタスクで `streamlit-js-eval` を追加する**（承認済み。`uv add streamlit-js-eval`）。

### 触るファイル

`runloop/persistence.py`（実装クラス）, `pyproject.toml`

### 依存

T14

### 完了条件

- [ ] AC-07-1「起点を指定すると、その座標がブラウザに保存される」（手動確認）
- [ ] AC-07-2「次回アプリを開いたとき、保存された起点が復元され地図上に表示される」（手動確認: ブラウザを閉じて開き直す）
- [ ] AC-07-3「復元された起点は、地図をクリックすることで上書きできる」（手動確認）
- [ ] `get_local_storage` ヘルパを使わず、汎用 JS 評価で 8.4 の3状態を自前で作る（ヘルパは「未読」と「保存なし」をどちらも `None` で返して区別できない）
- [ ] Python 側で二重にパースしている（戻り値は `JSON.stringify` の出力がそのまま入る `str`。design.md 8.4 の表）
- [ ] **書き込みの直後に `st.rerun()` を呼ばない**（`setItem` が取り消される報告がある。design.md 8.4）
- [ ] 座標を URL・ログ・エラーメッセージに出さない（design.md 8.7）
- [ ] localStorage への実際の読み書きは手動確認とし、自動テストを書かない（design.md 10.4）

### 8月12日のスパイク5との関係

要検証 #3 のうち「iOS が未使用のサイトデータを消すか」は 2026-08-12 頃に確認予定。
**方式が変わった場合の差し替え範囲はこのタスクだけ**で、T14 のポートと検証ロジックは変わらない。
なお URL クエリパラメータへの退避は解決にならない（URL も同様に失われる。design.md 11節 #3）。

### 参照

design.md 8.1 / 8.4 / 8.7 / 10.4 / 11節 #3、ADR-0004、requirements.md AC-07-1〜3

### ブランチ

`feat/persistence-localstorage`

---

## T16: ui/map_view.py（folium での地図描画）

状態: 未着手

### 目的

地図の描画を Streamlit 層の中で1ファイルに閉じる。

### 触るファイル

`ui/map_view.py`, `tests/test_map_view.py`

### 依存

T02（T11 の `Checkpoint` を表示に使う）

### 完了条件

- [ ] AC-05-2「設定された起点が地図上にマーカーで表示される」
- [ ] AC-01-1「コースが1本地図上に表示される」のうち描画部分（ループのポリラインとチェックポイントのマーカー）
- [ ] 生成された HTML に想定の座標が含まれることの確認（WORKFLOW.md フェーズ4 のテスト方針）
- [ ] 非機能要件「スマートフォンの縦画面で、指で操作できる大きさの部品を用いる」（手動確認）
- [ ] **接近区間の線を描かない**（レスポンスに存在しない。design.md 4.5）
- [ ] 地図の見た目そのものは手動確認とする（design.md 10.4）

### 参照

design.md 4.5 / 10.4 / 12節、requirements.md AC-01-1 / AC-05-2

### ブランチ

`feat/map-view`

---

## T17: ui/app.py — 起点の確定フローと3状態の描画

状態: 未着手

### 目的

起点をクリックして確定し、`/v2/snap` で1回プローブして保存するまで。
ここで距離を入れる前に不適切な起点を弾く。

### 触るファイル

`ui/app.py`, `runloop/config.py`（プロバイダの組み立て）

### 依存

T15, T16, T12, T07

### 完了条件

- [ ] AC-05-1「地図上をクリックすると、その位置が起点として設定される」
- [ ] AC-05-3「起点を指定せずに実行しようとした場合、指定を促すメッセージが表示される」
- [ ] AC-01-3 の確定時判定: スナップ距離が 300m 超、または 350m 以内に道路なし（`null`）のとき**起点として確定しない**。directions を**1回も送信しない**（design.md 4.6.3 / 10.2「確定時の拒否」）
- [ ] AC-07-4: `PENDING` の間は起点の案内も復元表示もせず、実行ボタンを無効にする（AC-07-2 と AC-07-4 が画面上で混ざらない。design.md 8.4）
- [ ] AC-06-2「API キーが未設定の場合、設定が必要である旨のメッセージが表示される」→ 最初の API 呼び出し（snap）の前に出る
- [ ] クリックごとではなく**「この地点を起点にする」の確定操作で1回だけ** snap を投げる（design.md 4.6.1）
- [ ] 接近ゲートを通った起点だけが保存される（design.md 8.2 の不変則）
- [ ] 画面に文字列リテラルを書かず、文言は `messages.py` から取る
- [ ] 画面は手動確認とする（design.md 10.4）

### 参照

design.md 4.6.1 / 4.6.3 / 8.2 / 8.4 / 9.1 / 10.2、requirements.md AC-01-3 / AC-05-1 / AC-05-3 / AC-06-2 / AC-07-4

### ブランチ

`feat/app-origin`

---

## T18a: ui/app.py — 実行と結果表示

状態: 未着手

### 目的

目標距離を入れて実行し、コースと距離とチェックポイントが出るところまで。
**このタスク完了時に一度、動くアプリとして手動確認する。**

### 触るファイル

`ui/app.py`

### 依存

T17, T13, T09, T11

### 完了条件

- [ ] AC-01-1「起点と目標距離を指定して実行すると、コースが1本地図上に表示される」
- [ ] AC-02-1「合計距離がキロメートル単位（小数第2位まで）で表示される」
- [ ] AC-02-2「距離誤差（合計距離 − 目標距離）がメートル単位で、符号付きで表示される」
- [ ] AC-02-3「合計距離が目標より短い場合、不足分を起点付近で調整する案内が表示される」
- [ ] AC-02-4「合計距離が目標より長い場合、超過分を手前で切り上げる案内が表示される」
- [ ] AC-02-5「接近距離が 50m を超える場合、その値を画面に表示し、ずれの向きも併せて伝える」
- [ ] AC-03-3「選ばれたコースの獲得標高が画面に表示される」（ループ区間のみの注記つき）
- [ ] AC-04-1「チェックポイントが最大5件表示される」
- [ ] AC-01-4 の妥協パスの表示（誤差最小の1本＋「条件を満たすコースが見つかりませんでした」）
- [ ] 状態は `st.session_state["run"]` に `RunSession` を1つ置くだけ（design.md 6.1）
- [ ] 表示する文言はすべて `messages.py` から取る
- [ ] 非機能要件「実行から結果表示までを10秒以内に完了する」を手元で1回実測した

### この時点で残る未実装（T18b で閉じる）

失敗経路の分岐（AC-06-1〜3 の表示）と予期しない例外の捕捉（AC-06-4）は T18b。
**T18a の段階では API 失敗時に Streamlit のトレースバックが画面に出る。**
これは中間状態として受け入れる。

### 参照

design.md 6.1 / 9.1 / 12節、requirements.md AC-01-1 / AC-01-4 / AC-02-1〜5 / AC-03-3 / AC-04-1 / 7節（性能）

### ブランチ

`feat/app-run`

---

## T18b: ui/app.py — 引き直しと例外処理

状態: 未着手

### 目的

引き直しを配線し、どの経路でもアプリが止まらない状態にする。

### 触るファイル

`ui/app.py`

### 依存

T18a

### 完了条件

- [ ] AC-08-1「引き直しを操作すると、次に良い候補が表示される」
- [ ] AC-08-2「引き直しでは外部 API を呼び出さない」（在庫から出していることを手動確認。呼び出し回数が増えない）
- [ ] AC-08-3「在庫を出し切った場合、それ以上の候補がない旨を表示する」＋「もう一度探す」を**同じ画面に置く**（design.md 6.3）
- [ ] AC-06-1「API 呼び出しが失敗した場合、失敗した旨のメッセージが表示される」→ 429／5xx／接続不能で design.md 9.1 の文言が出る
- [ ] AC-06-3「候補が1件も取得できなかった場合、その旨のメッセージが表示される」（全滅・異常値除外で0件の両方）
- [ ] AC-06-4「いずれの異常時も、アプリが停止せず再実行できる状態を保つ」→ `RouteProviderError` とその他の例外をそれぞれ捕まえ、**トレースバックを画面に出さない**。入力（起点・目標距離）を保持する
- [ ] 例外の型・ステータス・残数・失敗の内訳はログへ。画面には行動可能な文言だけ（design.md 9.2）
- [ ] AC-08-3 のメッセージを例外処理ではなく**通常の経路**として作っている（悲観側では2本以上が81.4%。design.md 6.3）

### 参照

design.md 6.2 / 6.3 / 9.1 / 9.2、requirements.md AC-06-1 / AC-06-3 / AC-06-4 / AC-08-1〜3

### ブランチ

`feat/app-reroll-errors`

---

## T19: 結合確認（受け入基準の全件チェックと異常系）

状態: 未着手

### 目的

受け入基準を上から順に実画面で確認し、性能と API 消費の前提が実際に成立していることを
確かめる。

### 依存

T18b

### 完了条件

- [ ] AC-01-1〜AC-08-4 の全件を手動チェックリストで通した（WORKFLOW.md フェーズ6）
- [ ] 非機能要件「実行から結果表示までを10秒以内に完了する」を実測した（要検証 #10: Community Cloud での同時14本はデプロイ後）
- [ ] 非機能要件「1回の実行における外部 API 呼び出しを 15 回以内に抑える」を、残数ヘッダの推移で確認した
- [ ] 非機能要件「毎分の呼び出しを 40 回未満に抑える」を、連続実行で確認した
- [ ] 極端な値（0.1km / 100km）と、道路から離れた地点のクリックが適切に扱われる
- [ ] `/code-review` と `/security-review` を流し、指摘に対応したか対応しない理由が説明できる（WORKFLOW.md フェーズ5）
- [ ] 設計から逸脱した点があれば `docs/design.md` を更新した

### 参照

WORKFLOW.md フェーズ5・6、design.md 11節、requirements.md 5節・7節

### ブランチ

`test/integration-check`
